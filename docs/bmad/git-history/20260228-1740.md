# Git Report — 20260228-1740

## Branche : main

### Commits effectués

| Hash | Type | Scope | Description |
|------|------|-------|-------------|
| `6adfb7a` | chore | cleanup | remove dead code — unused screen, theme tokens, orphan scripts |
| `ee42aad` | feat | web | add dynamic apple icon via Next.js ImageResponse |
| `8fa42db` | fix | web | disable webpack fs cache in dev on Windows to prevent EPERM |
| `496782d` | fix | web | replace `<a>` with Next.js `<Link>` in SubscribeSection privacy link |
| `d890a15` | chore | — | remove empty .mcp.json |

### Fichiers modifiés

| Fichier | Action |
|---------|--------|
| `mobile/src/screens/StatsRepartitionScreen.tsx` | supprimé (écran orphelin) |
| `mobile/src/screens/__tests__/StatsRepartitionScreen.test.tsx` | supprimé |
| `mobile/src/theme/index.ts` | modifié — 6 tokens morts retirés |
| `web/src/app/apple-icon.tsx` | ajouté — dynamic apple icon (ImageResponse) |
| `web/public/manifest.webmanifest` | modifié — apple-touch-icon → apple-icon |
| `web/src/app/layout.tsx` | modifié — apple-touch-icon → apple-icon |
| `web/next.config.ts` | modifié — webpack cache désactivé en dev sur Windows |
| `web/src/components/sections/SubscribeSection.tsx` | modifié — `<a>` → `<Link>` |
| `docs/bmad/do/20260228-1730-clean-dead-code.md` | ajouté — rapport nettoyage |

### Fichiers exclus (unstaged)

| Fichier | Raison |
|---------|--------|
| 25 scripts/logs racine | Non trackés, scripts de debug ponctuels supprimés |

### Vérifications

- TypeScript (mobile) : ✅ 0 erreur
- TypeScript (web) : ✅ 0 erreur
- Tests : ✅ 1559 passed / 93 suites
- Push : ✅ `46247e0..d890a15  main -> main`

### Résumé

Nettoyage du code mort (écran orphelin, 6 tokens thème, 25 scripts debug) + améliorations web (apple icon dynamique, fix webpack Windows, fix Link Next.js).
