# Git Report — 20260226-2135

## Branche : main

### Commits effectués
| Hash | Type | Scope | Description |
|------|------|-------|-------------|
| d741221 | chore | app | rename app GymTracker → Kore + new logo assets |

### Fichiers modifiés
| Fichier | Action |
|---------|--------|
| mobile/app.json | modifié — name: "Kore" |
| mobile/assets/icon.png | remplacé — logo Kore 1024×1024 |
| mobile/assets/adaptive-icon.png | remplacé — logo Kore avec safe-zone Android |
| mobile/assets/splash-icon.png | remplacé — logo Kore centré splash |
| mobile/assets/favicon.png | remplacé — logo Kore 196×196 |

### Corrections silencieuses (déjà en HEAD)
| Fichier | Problème trouvé et corrigé |
|---------|--------------------------|
| mobile/src/model/models/Exercise.ts | `database.batch(batch)` → `database.batch(...batch)` |
| mobile/src/model/utils/programImportUtils.ts | `database.batch(batch)` → `database.batch(...batch)` |

> Ces fichiers avaient régressé (session précédente non commitée). Le fix les a restaurés à l'état HEAD. 4 tests qui échouaient → 1259/1259 ✅

### Fichiers exclus (unstaged)
| Fichier | Raison |
|---------|--------|
| .gitignore | autre session |
| docs/bmad/do/*.md | autre session |
| mobile/src/model/seed.ts | autre session |
| mobile/src/model/seedDevData.ts | autre session |

### Vérifications
- TypeScript : ✅ zéro erreur
- Tests : ✅ 1259/1259 passed (4 failing → corrigés avant commit)
- Push : ✅ remote à jour

### Résumé
Logo Kore (K gradient violet→cyan) intégré dans les assets Android (icon, adaptive-icon, splash) + renommage app "Kore".
