# Code Review — 20260226-1815

## Fichiers analysés
| Fichier | Lignes modifiées | Verdict |
|---------|-----------------|---------|
| `.gitignore` | +2 | ✅ |
| `web/src/app/layout.tsx` | +1 / -1 | ✅ |
| `web/src/app/api/subscribe/route.ts` | +1 / -1 | 🟡 |
| `web/src/emails/welcome.tsx` | +120 / -80 | 🟡 |
| `mobile/run_checks.ps1` | +2 / -2 | 🟡 |

## Problèmes trouvés
| # | Fichier | Ligne | Problème | Sévérité |
|---|---------|-------|----------|----------|
| 1 | `mobile/run_checks.ps1` | 9 | `--testPathPattern="WorkoutExerciseCard\|SessionExerciseItem"` — scope de test réduit à 2 fichiers, probablement laissé par erreur après dev WorkoutExerciseCard. Quiconque exécute ce script croit lancer toute la suite. | 🟡 |
| 2 | `web/src/app/api/subscribe/route.ts` | 34 | Sender `onboarding@resend.dev` (sandbox Resend) en dur dans le code. Fonctionnel en free tier mais pas le domaine de marque. À externaliser en env var ou documenter si domaine custom activé un jour. | 🟡 |
| 3 | `web/src/emails/welcome.tsx` | 244 | Unsubscribe par "réponds à cet email" au lieu d'un lien `%unsubscribe_url%`. Acceptable pour email transactionnel de bienvenue (RGPD), mais à revoir si l'email devient marketing. | 🟡 |

## Points positifs
- `.gitignore` : ajouts standards et nécessaires (`.vercel`, `.env*.local`)
- `layout.tsx` : retouche favicon SVG cosmétique, propre
- `welcome.tsx` : structure `<table role="presentation">` email-safe correcte, styles inline, palette cohérente avec le site neumorphique, emojis encodés en unicode (`\u{...}`)
- `route.ts` : modification minimale, chirurgicale

## Verdict global : PUSH ✅

Aucun problème bloquant. Les 3 issues sont cosmétiques/opérationnelles.
Recommandation : corriger `run_checks.ps1` pour le remettre en mode "tous les tests" avant que quelqu'un d'autre ne s'en serve.
