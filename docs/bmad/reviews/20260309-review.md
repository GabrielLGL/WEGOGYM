# Code Review — 20260309

## Fichiers analysés
| Fichier | Lignes modifiées | Verdict |
|---------|-----------------|---------|
| mobile/.easignore | +5 / -0 | ✅ |
| mobile/src/components/CoachMarks.tsx | +38 / -3 | ✅ |
| mobile/src/screens/OnboardingScreen.tsx | +1 / -1 | ✅ |
| mobile/src/screens/__tests__/OnboardingScreen.test.tsx | +5 / -4 | ✅ |

## Problèmes trouvés
| # | Fichier | Ligne | Problème | Sévérité |
|---|---------|-------|----------|----------|
| 1 | CoachMarks.tsx | 66-67 | Constantes MAX_MEASURE_RETRIES / MEASURE_RETRY_DELAY dans le corps du composant — pourraient être module-level | 🟡 |

## Détails

### .easignore
Exclut `android/app/build/`, `android/.gradle/`, `android/build/` du bundle EAS. Réduit l'archive de ~1.7 GB. Ces dossiers sont recréés par EAS Build.

### CoachMarks.tsx
Ajout d'un mécanisme de retry pour `measureInWindow` :
- Retry jusqu'à 5 fois avec 300ms entre chaque tentative
- Auto-dismiss si la mesure échoue après tous les retries
- Timers correctement nettoyés dans les cleanups useEffect
- Delay initial augmenté de 400 à 600ms

### OnboardingScreen.tsx
`navigation.replace('Home')` → `navigation.reset(...)` — empêche le retour arrière vers l'onboarding via le bouton back.

### OnboardingScreen.test.tsx
Mock et assertions mis à jour pour refléter le changement replace → reset.

## Verdict global : PUSH ✅
