<!-- v1.0 — 2026-02-27 -->
# Rapport — privacy-rgpd — Groupe B — 20260227-0030

## Objectif
Mettre à jour la landing page et le sitemap pour intégrer la page de Politique de confidentialité :
1. Ajouter un lien "Confidentialité" dans le footer
2. Ajouter une mention RGPD avec lien sous le formulaire d'inscription
3. Ajouter `/privacy` dans le sitemap

## Fichiers concernés
- `web/src/app/page.tsx` — modifier footer (ligne ~380) et formulaire (ligne ~370)
- `web/src/app/sitemap.ts` — ajouter entrée `/privacy`

## Contexte technique

### Stack
- Next.js 15 App Router, TypeScript strict
- `web/src/app/page.tsx` est un **Client Component** (`"use client"` en ligne 1)
- `web/src/app/sitemap.ts` est un module de génération Next.js (MetadataRoute.Sitemap)

### Design CSS du site (à respecter)
```
var(--text-muted)   → texte secondaire/grisé
var(--accent)       → couleur accent
```

Classes Tailwind sur les liens du footer existants :
```
"text-[var(--text-muted)] text-sm font-medium no-underline hover:text-[var(--accent)] transition-colors"
```

## Étapes

### 1. Modifier `web/src/app/page.tsx`

#### Changement A — Lien dans le footer (ligne ~379–388)

Code actuel du footer :
```tsx
<div className="flex justify-center gap-6 mb-6 flex-wrap">
  <a href="#features" className="text-[var(--text-muted)] text-sm font-medium no-underline hover:text-[var(--accent)] transition-colors">
    Fonctionnalites
  </a>
  <a href="#pricing" className="text-[var(--text-muted)] text-sm font-medium no-underline hover:text-[var(--accent)] transition-colors">
    Tarifs
  </a>
  <a href="mailto:contact@kore-app.com" className="text-[var(--text-muted)] text-sm font-medium no-underline hover:text-[var(--accent)] transition-colors">
    Contact
  </a>
</div>
```

Code à obtenir (ajouter le lien Confidentialité) :
```tsx
<div className="flex justify-center gap-6 mb-6 flex-wrap">
  <a href="#features" className="text-[var(--text-muted)] text-sm font-medium no-underline hover:text-[var(--accent)] transition-colors">
    Fonctionnalites
  </a>
  <a href="#pricing" className="text-[var(--text-muted)] text-sm font-medium no-underline hover:text-[var(--accent)] transition-colors">
    Tarifs
  </a>
  <a href="mailto:contact@kore-app.com" className="text-[var(--text-muted)] text-sm font-medium no-underline hover:text-[var(--accent)] transition-colors">
    Contact
  </a>
  <a href="/privacy" className="text-[var(--text-muted)] text-sm font-medium no-underline hover:text-[var(--accent)] transition-colors">
    Confidentialite
  </a>
</div>
```

#### Changement B — Mention RGPD sous le formulaire (ligne ~370)

Code actuel :
```tsx
<p className="reveal text-[var(--text-muted)] text-xs mt-8 opacity-80">
  Pas de spam. Desabonnement en un clic.
</p>
```

Code à obtenir :
```tsx
<p className="reveal text-[var(--text-muted)] text-xs mt-8 opacity-80">
  Pas de spam. Desabonnement en un clic.{" "}
  <a href="/privacy" className="underline hover:text-[var(--accent)] transition-colors">
    Politique de confidentialite
  </a>
</p>
```

### 2. Modifier `web/src/app/sitemap.ts`

Code actuel :
```ts
import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: "https://kore-app.com",
      lastModified: new Date(),
      changeFrequency: "monthly",
      priority: 1,
    },
  ];
}
```

Code à obtenir :
```ts
import type { MetadataRoute } from "next";

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: "https://kore-app.com",
      lastModified: new Date(),
      changeFrequency: "monthly",
      priority: 1,
    },
    {
      url: "https://kore-app.com/privacy",
      lastModified: new Date(),
      changeFrequency: "yearly",
      priority: 0.3,
    },
  ];
}
```

## Contraintes
- Ne modifier QUE les 3 zones décrites ci-dessus — ne pas toucher au reste de page.tsx
- Respecter les CSS vars existants — aucune couleur hardcodée
- Conserver la syntaxe des caractères encodés (ex: `Desabonnement` sans accent car le reste du fichier est cohérent)
- Texte en français, cohérent avec le style du site
- Pas d'import supplémentaire nécessaire (les `<a>` standard suffisent, pas de `Link` next/link car page.tsx utilise déjà des `<a>`)

## Critères de validation
- `cd web && npx tsc --noEmit` → 0 erreur TypeScript
- `npm test` → 0 fail
- Lien "Confidentialite" visible dans le footer de `http://localhost:3000`
- Mention "Politique de confidentialite" cliquable sous le formulaire
- `http://localhost:3000/sitemap.xml` contient `https://kore-app.com/privacy`

## Dépendances
Aucune dépendance entre groupes — peut être exécuté en parallèle avec Groupe A.

## Statut
✅ Résolu — 20260227-0033

## Résolution
Rapport do : docs/bmad/do/20260227-0033-feat-privacy-footer-sitemap.md
