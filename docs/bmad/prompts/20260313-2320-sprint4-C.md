<!-- v1.0 — 2026-03-13 -->
# Rapport — Stats Hexagone / Radar Chart — Groupe C — 20260313-2320

## Objectif
Créer un composant `HexagonStatsCard` (radar chart en pentagone — 5 axes) qui visualise le profil athlétique de l'utilisateur, et un écran dédié `StatsHexagonScreen` accessible depuis HomeScreen (nouvelle tuile dans la section "Statistiques"). Les 5 axes sont calculés depuis les données existantes sans migration DB.

## Fichiers concernés
- `mobile/src/components/HexagonStatsCard.tsx` — NOUVEAU : composant radar chart SVG
- `mobile/src/screens/StatsHexagonScreen.tsx` — NOUVEAU : écran d'analyse complète
- `mobile/src/navigation/index.tsx` — MODIFIER : ajouter route `StatsHexagon`
- `mobile/src/screens/HomeScreen.tsx` — MODIFIER : tuile dans section "Statistiques"
- `mobile/src/i18n/fr.ts` — MODIFIER : clés `hexagon.*`
- `mobile/src/i18n/en.ts` — MODIFIER : clés `hexagon.*`

## Contexte technique

### Stack & Architecture
- React Native + Expo 52 + TypeScript strict (Fabric / New Architecture)
- **`react-native-svg` 15.8.0 est disponible** — utiliser `Svg`, `Polygon`, `Text as SvgText`, `Line`, `Circle` depuis `react-native-svg`
- `useColors()` → `ThemeColors` depuis `mobile/src/contexts/ThemeContext.tsx`
- `useLanguage()` → `{ t }` depuis `mobile/src/contexts/LanguageContext.tsx`
- `useDeferredMount()` depuis `mobile/src/hooks/useDeferredMount.ts`
- `withObservables` + `observeCurrentUser()` depuis `mobile/src/model/utils/databaseHelpers.ts`

### Modèles disponibles
```ts
// User — table 'users'
user.totalPrs: number         // PRs totaux
user.totalTonnage: number     // tonnage total en kg
user.currentStreak: number    // streak actuel en semaines
user.bestStreak: number       // meilleur streak en semaines
user.level: number            // niveau XP

// Set — table 'sets' (pour calcul distinct muscles)
set.exercise: Relation<Exercise>
exercise.muscles: string[]

// History — table 'histories' (pour sessions count)
history.deletedAt: Date | null
history.isAbandoned: boolean
```

## Les 5 Axes (et leur normalisation)

| Axe | Source | Valeur max (= 1.0) |
|-----|--------|-------------------|
| **Force** ⚡ | `user.totalPrs` | 100 PRs |
| **Endurance** 🔥 | `user.totalTonnage` (kg) | 100 000 kg |
| **Volume** 📊 | `histories.length` (total séances) | 200 séances |
| **Régularité** 📅 | `user.bestStreak` (semaines) | 52 semaines |
| **Équilibre** ⚖️ | Nombre de groupes musculaires distincts pratiqués | 9 groupes |

Normalisation : `Math.min(1.0, valeur / max)` — **pas de dépassement au-delà de 1.0**.

## Implémentation

### hexagonHelpers.ts (intégré dans le composant ou fichier séparé)
```ts
interface HexagonAxis {
  label: string         // "Force", "Endurance", etc.
  icon: string          // Ionicons name
  value: number         // 0-1 normalisé
  rawValue: number      // valeur brute
  rawMax: number        // valeur max de référence
  color: string         // couleur d'accent
}

function computeHexagonAxes(
  user: User,
  totalHistories: number,
  distinctMuscleGroups: number,
  colors: ThemeColors,
): HexagonAxis[]
```

### HexagonStatsCard.tsx — Pentagone SVG

**Géométrie d'un pentagone régulier :**
```ts
// 5 sommets disposés en cercle, axe 0 vers le haut (angle = -90° + i * 72°)
const N = 5
const cx = SIZE / 2, cy = SIZE / 2
const R = SIZE * 0.4  // rayon max

function getPoint(i: number, radius: number): { x: number; y: number } {
  const angle = (Math.PI * 2 * i) / N - Math.PI / 2
  return { x: cx + radius * Math.cos(angle), y: cy + radius * Math.sin(angle) }
}
```

**Rendu SVG :**
1. Grilles concentriques (3 pentagones à 33%, 66%, 100% du rayon) — `colors.separator`
2. Lignes des axes (du centre à chaque sommet) — `colors.separator`
3. Polygone rempli avec les valeurs utilisateur (opacity 0.3, couleur `colors.primary`)
4. Contour du polygone (`colors.primary`, strokeWidth 2)
5. Labels des axes avec icônes en dehors du pentagone
6. Valeur en % au centre

**Props HexagonStatsCard :**
```tsx
interface HexagonStatsCardProps {
  axes: HexagonAxis[]
  size?: number  // défaut 280
}
```

### StatsHexagonScreen.tsx — Écran complet

```
ScrollView:
  - Titre / sous-titre
  - <HexagonStatsCard axes={axes} size={280} />

  - Section "Détail par axe" :
    Pour chaque axe :
    ┌──────────────────────────────────────┐
    │ ⚡ Force         ████████░░  75%     │
    │  75 PRs sur 100                      │
    └──────────────────────────────────────┘
    (barre de progression + valeur brute / max)
```

**withObservables pattern :**
```ts
const enhance = withObservables([], () => ({
  user: observeCurrentUser(),
  sets: database.get<WorkoutSet>('sets').query(
    Q.on('histories', Q.and(
      Q.where('deleted_at', null),
      Q.or(Q.where('is_abandoned', null), Q.where('is_abandoned', false)),
    ))
  ).observe(),
  exercises: database.get<Exercise>('exercises').query().observe(),
  histories: database.get<History>('histories').query(
    Q.where('deleted_at', null),
    Q.or(Q.where('is_abandoned', null), Q.where('is_abandoned', false)),
  ).observe(),
}))
```

**Calculs dans useMemo :**
- `distinctMuscleGroups` : compter les groupes musculaires uniques dans tous les sets (via exercises)
- `axes` : appeler `computeHexagonAxes(user, histories.length, distinctMuscleGroups, colors)`

### Navigation — modifier navigation/index.tsx
**Lazy import** (après `SkillTreeScreen`) :
```ts
const StatsHexagonScreen = React.lazy(() => import('../screens/StatsHexagonScreen'))
```

**RootStackParamList** (après `SkillTree: undefined`) :
```ts
StatsHexagon: undefined;
```

**Stack.Screen** (après la ligne SkillTree) :
```tsx
<Stack.Screen name="StatsHexagon" component={StatsHexagonScreen} options={{ title: t.navigation.statsHexagon }} />
```

### HomeScreen — ajouter tuile dans section "Statistiques"
Dans la définition de `SECTIONS` (autour de la ligne 146), dans le 2ème objet `{ title: t.home.sections.stats, tiles: [...] }`, ajouter en dernier :
```ts
{ icon: 'git-network-outline', label: t.home.tiles.hexagon, route: 'StatsHexagon' },
```

### Traductions

**fr.ts — dans `home.tiles`** (après `skillTree: 'Skill Tree'`) :
```ts
hexagon: 'Profil Athlète',
```

**fr.ts — dans `navigation`** (après `skillTree: 'Skill Tree'`) :
```ts
statsHexagon: 'Profil Athlète',
```

**fr.ts — nouvelle section (avant `share: {`) :**
```ts
hexagon: {
  title: 'Profil Athlète',
  subtitle: 'Tes 5 dimensions athlétiques',
  axes: {
    force: 'Force',
    endurance: 'Endurance',
    volume: 'Volume',
    regularite: 'Régularité',
    equilibre: 'Équilibre',
  },
  detailSection: 'Détail par axe',
  outOf: 'sur',
},
```

**en.ts — mêmes clés** :
```ts
hexagon: {
  title: 'Athletic Profile',
  subtitle: 'Your 5 athletic dimensions',
  axes: {
    force: 'Strength',
    endurance: 'Endurance',
    volume: 'Volume',
    regularite: 'Consistency',
    equilibre: 'Balance',
  },
  detailSection: 'Detail by axis',
  outOf: 'out of',
},
```

## Contraintes
- Utiliser `react-native-svg` (déjà installé, version 15.8.0) — import: `import { Svg, Polygon, Line, Text as SvgText, Circle } from 'react-native-svg'`
- Pas de dépendances npm supplémentaires
- Normalisation clampée à [0, 1] — jamais de dépassement visuel
- Toutes les couleurs via `colors.*` (sauf le remplissage du polygon qui peut utiliser `colors.primary + '4D'` pour 30% d'opacité)
- Respecter `spacing.*`, `borderRadius.*`, `fontSize.*` du thème

## Critères de validation
- `npx tsc --noEmit` → zéro erreur TypeScript
- Le pentagone se dessine correctement (pas de crash SVG)
- Tuile visible sur HomeScreen section "Statistiques"
- Navigation vers l'écran fonctionne
- Valeurs 0 → polygone minuscule visible (pas de crash)

## Dépendances
Aucune dépendance sur les autres groupes.

## Git — commit après implémentation
```bash
git add mobile/src/components/HexagonStatsCard.tsx \
        mobile/src/screens/StatsHexagonScreen.tsx \
        mobile/src/navigation/index.tsx \
        mobile/src/screens/HomeScreen.tsx \
        mobile/src/i18n/fr.ts \
        mobile/src/i18n/en.ts \
        docs/bmad/do/[rapport]
git commit -m "feat(stats): hexagon athletic profile — 5-axis radar chart (SVG)"
git push origin develop
# Si remote ahead : git pull --rebase puis re-push
```

## Statut
⏳ En attente
