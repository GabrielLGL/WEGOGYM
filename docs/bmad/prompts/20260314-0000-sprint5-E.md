<!-- v1.0 — 2026-03-14 -->
# Rapport — Exercice de la Semaine (#74) — Groupe E — 20260314-0000

## Objectif
Ajouter une carte "Exercice de la Semaine" dans la section outils du HomeScreen. Chaque semaine, suggère un exercice que l'utilisateur n'a pas fait depuis longtemps (ou jamais). Tap sur la carte → navigation vers `ExerciseDetail` si disponible, sinon affiche une BottomSheet avec les infos de l'exercice.

## Fichiers concernés
- `mobile/src/model/utils/exerciseOfWeekHelpers.ts` — NOUVEAU : logique de sélection
- `mobile/src/screens/HomeScreen.tsx` — MODIFIER : ajouter la carte dans la section outils
- `mobile/src/i18n/fr.ts` — MODIFIER : ajouter section `exerciseOfWeek.*`
- `mobile/src/i18n/en.ts` — MODIFIER : même structure

## Contexte technique

### Stack & Architecture
- React Native + Expo 52 + TypeScript strict (Fabric / New Architecture)
- WatermelonDB (offline-first) — données déjà disponibles dans HomeScreenBase
- `useColors()` → `ThemeColors` depuis `mobile/src/contexts/ThemeContext.tsx`
- `useLanguage()` → `{ t }` depuis `mobile/src/contexts/LanguageContext.tsx`
- Portal pattern pour la BottomSheet : `<BottomSheet>` depuis `mobile/src/components/BottomSheet.tsx`
- `useModalState()` depuis `mobile/src/hooks/useModalState.ts`
- **Ne pas modifier les `withObservables` ni les props existantes de HomeScreen**

### Données disponibles dans HomeScreenBase (props existantes)
```ts
sets: WorkoutSet[]        // tous les sets (avec exercise relation)
exercises: Exercise[]     // tous les exercices (id, name, muscles: string[])
histories: History[]      // pour filtrer les sets actifs
```

### Structure Exercise
```ts
exercise.id: string
exercise.name: string
exercise.muscles: string[]   // ex: ['Pecs', 'Triceps']
```

## Implémentation

### exerciseOfWeekHelpers.ts
```ts
export interface ExerciseOfWeekResult {
  exercise: Exercise
  lastDoneMs: number | null   // timestamp du dernier set pour cet exercice, null si jamais fait
  daysSinceLastDone: number | null
  isNew: boolean              // jamais fait par l'utilisateur
}

/**
 * Sélectionne l'exercice de la semaine.
 * Logique :
 * 1. Calculer la dernière date d'utilisation de chaque exercice (depuis sets)
 * 2. Priorité 1 : exercices jamais utilisés (isNew)
 * 3. Priorité 2 : exercices non utilisés depuis > 30 jours
 * 4. Parmi les candidats, choisir de façon déterministe selon la semaine courante
 *    (Math.floor(Date.now() / WEEK_MS) % candidates.length) pour que ça change chaque semaine
 * 5. Si tous les exercices ont été faits récemment → prendre le moins récent
 * 6. Retourner null si moins de 5 exercices disponibles au total
 */
export function computeExerciseOfWeek(
  exercises: Exercise[],
  sets: WorkoutSet[],
): ExerciseOfWeekResult | null
```

**Algorithme détaillé :**
```ts
import { WEEK_MS } from '../model/constants'

export function computeExerciseOfWeek(
  exercises: Exercise[],
  sets: WorkoutSet[],
): ExerciseOfWeekResult | null {
  if (exercises.length < 5) return null

  // 1. Construire map exerciseId → dernière date
  const lastUsed = new Map<string, number>()
  for (const set of sets) {
    const exId = set.exercise?.id
    if (!exId) continue
    const t = set.createdAt?.getTime() ?? 0
    const current = lastUsed.get(exId) ?? 0
    if (t > current) lastUsed.set(exId, t)
  }

  const now = Date.now()
  const thirtyDaysMs = 30 * 24 * 60 * 60 * 1000

  // 2. Catégoriser
  const newExercises = exercises.filter(e => !lastUsed.has(e.id))
  const staleExercises = exercises.filter(e => {
    const last = lastUsed.get(e.id)
    return last !== undefined && (now - last) > thirtyDaysMs
  })

  const candidates = newExercises.length > 0
    ? newExercises
    : staleExercises.length > 0
    ? staleExercises
    : [...exercises].sort((a, b) => (lastUsed.get(a.id) ?? 0) - (lastUsed.get(b.id) ?? 0))

  // 3. Sélection déterministe par semaine
  const weekIndex = Math.floor(now / WEEK_MS)
  const chosen = candidates[weekIndex % candidates.length]
  const lastMs = lastUsed.get(chosen.id) ?? null
  const daysSince = lastMs !== null ? Math.floor((now - lastMs) / (24 * 60 * 60 * 1000)) : null

  return {
    exercise: chosen,
    lastDoneMs: lastMs,
    daysSinceLastDone: daysSince,
    isNew: !lastUsed.has(chosen.id),
  }
}
```

### Intégration dans HomeScreen

**Import à ajouter :**
```ts
import { computeExerciseOfWeek } from '../model/utils/exerciseOfWeekHelpers'
import type { ExerciseOfWeekResult } from '../model/utils/exerciseOfWeekHelpers'
```

**useMemo dans HomeScreenBase** (après les flashbacks) :
```ts
const exerciseOfWeek = useMemo(
  () => computeExerciseOfWeek(exercises, sets),
  [exercises, sets],
)
```

**État modal :**
```ts
const exerciseModal = useModalState()
```

**Placement dans le JSX :** Ajouter dans la section `{/* ── Tuiles Outils ── */}` (vers la ligne 652), **après** les boutons Leaderboard et Skill Tree :

```tsx
{/* ── Exercice de la Semaine ── */}
{exerciseOfWeek && (
  <TouchableOpacity
    style={styles.exerciseOfWeekCard}
    onPress={() => { haptics.onPress(); exerciseModal.open() }}
    activeOpacity={0.8}
  >
    <View style={styles.exerciseOfWeekHeader}>
      <Ionicons name="sparkles-outline" size={18} color={colors.primary} />
      <Text style={styles.exerciseOfWeekTitle}>{t.exerciseOfWeek.title}</Text>
    </View>
    <Text style={styles.exerciseOfWeekName}>{exerciseOfWeek.exercise.name}</Text>
    <Text style={styles.exerciseOfWeekSub}>
      {exerciseOfWeek.isNew
        ? t.exerciseOfWeek.neverDone
        : t.exerciseOfWeek.daysAgo.replace('{n}', String(exerciseOfWeek.daysSinceLastDone))}
    </Text>
    {exerciseOfWeek.exercise.muscles.length > 0 && (
      <View style={styles.exerciseOfWeekMuscles}>
        {exerciseOfWeek.exercise.muscles.slice(0, 3).map(m => (
          <View key={m} style={styles.muscleChip}>
            <Text style={styles.muscleChipText}>{m}</Text>
          </View>
        ))}
      </View>
    )}
  </TouchableOpacity>
)}

{/* Modal detail exercice */}
<BottomSheet
  visible={exerciseModal.isOpen}
  onClose={exerciseModal.close}
  title={exerciseOfWeek?.exercise.name ?? ''}
>
  {exerciseOfWeek && (
    <View style={styles.exerciseModalContent}>
      <Text style={styles.exerciseModalMuscles}>
        {exerciseOfWeek.exercise.muscles.join(' · ')}
      </Text>
      <Text style={styles.exerciseModalHint}>
        {exerciseOfWeek.isNew
          ? t.exerciseOfWeek.tryItNever
          : t.exerciseOfWeek.tryItAgain.replace('{n}', String(exerciseOfWeek.daysSinceLastDone))}
      </Text>
    </View>
  )}
</BottomSheet>
```

**Styles à ajouter dans StyleSheet :**
```ts
exerciseOfWeekCard: {
  backgroundColor: colors.card,
  borderRadius: borderRadius.lg,
  padding: spacing.md,
  marginHorizontal: spacing.md,
  marginTop: spacing.sm,
  borderWidth: 1,
  borderColor: colors.separator,
},
exerciseOfWeekHeader: {
  flexDirection: 'row',
  alignItems: 'center',
  gap: spacing.xs,
  marginBottom: spacing.xs,
},
exerciseOfWeekTitle: {
  fontSize: fontSize.xs,
  color: colors.primary,
  fontWeight: '600',
  textTransform: 'uppercase',
  letterSpacing: 0.5,
},
exerciseOfWeekName: {
  fontSize: fontSize.md,
  color: colors.text,
  fontWeight: '700',
  marginBottom: spacing.xs,
},
exerciseOfWeekSub: {
  fontSize: fontSize.xs,
  color: colors.textSecondary,
  marginBottom: spacing.sm,
},
exerciseOfWeekMuscles: {
  flexDirection: 'row',
  flexWrap: 'wrap',
  gap: spacing.xs,
},
exerciseModalContent: {
  padding: spacing.md,
},
exerciseModalMuscles: {
  fontSize: fontSize.sm,
  color: colors.textSecondary,
  marginBottom: spacing.sm,
},
exerciseModalHint: {
  fontSize: fontSize.sm,
  color: colors.text,
},
```

**Note :** Le style `muscleChip` et `muscleChipText` existe probablement déjà dans HomeScreen. Si oui, réutiliser — sinon ajouter.

### Traductions

**fr.ts — nouvelle section (avant `share: {`) :**
```ts
exerciseOfWeek: {
  title: 'Exercice de la semaine',
  neverDone: 'Jamais essayé',
  daysAgo: 'Il y a {n} jours',
  tryItNever: 'Tu n\'as jamais essayé cet exercice — c\'est le moment !',
  tryItAgain: 'Tu ne l\'as pas fait depuis {n} jours — ressaie !',
},
```

**en.ts — mêmes clés :**
```ts
exerciseOfWeek: {
  title: 'Exercise of the week',
  neverDone: 'Never tried',
  daysAgo: '{n} days ago',
  tryItNever: 'You\'ve never tried this exercise — now is the time!',
  tryItAgain: 'You haven\'t done it in {n} days — try again!',
},
```

## Contraintes
- **Ne pas modifier les `withObservables` ni les props de HomeScreenBase** — utiliser `exercises` et `sets` déjà disponibles
- Retourner `null` si < 5 exercices (carte invisible)
- Sélection déterministe par semaine (pas de `Math.random()`)
- Pas de dépendances npm supplémentaires
- `BottomSheet` depuis `mobile/src/components/BottomSheet.tsx` (jamais `<Modal>` natif)
- Respecter `spacing.*`, `borderRadius.*`, `fontSize.*`

## Critères de validation
- `npx tsc --noEmit` → zéro erreur TypeScript
- La carte "Exercice de la semaine" apparaît dans la section outils de HomeScreen
- L'exercice change d'une semaine à l'autre (mais reste fixe pendant la semaine)
- La BottomSheet s'ouvre au tap avec le nom et les muscles
- Invisible pour un utilisateur avec < 5 exercices

## Dépendances
Aucune dépendance sur les autres groupes.

## Git — commit après implémentation
```bash
git add mobile/src/model/utils/exerciseOfWeekHelpers.ts \
        mobile/src/screens/HomeScreen.tsx \
        mobile/src/i18n/fr.ts \
        mobile/src/i18n/en.ts \
        docs/bmad/do/[rapport]
git commit -m "feat(home): exercise of the week card — weekly suggestion of underused exercises"
git push origin develop
# Si remote ahead : git pull --rebase puis re-push
```

## Statut
✅ Résolu — 20260314-0100

## Résolution
Rapport do : docs/bmad/do/20260314-0100-feat-exercise-of-week.md
