<!-- v1.0 — 2026-03-11 -->
# Rapport — Partage workout/PR/badge — Groupe A — 20260311-2130

## Objectif
Créer l'infrastructure de partage : service de génération de contenu texte/image, installation de `react-native-view-shot`, et ajout des clés i18n.

## Fichiers concernés
- `mobile/src/services/shareService.ts` (NOUVEAU)
- `mobile/src/i18n/fr.ts` (MODIFIER — ajouter clés share)
- `mobile/src/i18n/en.ts` (MODIFIER — ajouter clés share)
- `mobile/package.json` (MODIFIER — ajouter dépendance)

## Contexte technique
- `expo-sharing` (v13.0.1) est déjà installé — utiliser `Sharing.shareAsync()` pour le dialogue natif.
- `expo-file-system` est déjà installé — pour écrire les images temporaires.
- Pattern existant dans `SettingsDataSection.tsx` : `Sharing.shareAsync(fileUri, { mimeType, dialogTitle })`.
- i18n custom léger : `fr.ts` et `en.ts` sont des objets plats, pas de `as const` sur fr.ts. Utiliser `useLanguage()` → `{ t }`.
- Le service doit être un module utilitaire pur (pas un hook), exportant des fonctions.

## Étapes

### 1. Installer `react-native-view-shot`
```bash
cd mobile && npx expo install react-native-view-shot
```

### 2. Créer `mobile/src/services/shareService.ts`

Le service expose 3 fonctions principales :

```typescript
// Types
interface WorkoutShareData {
  durationSeconds: number
  totalVolume: number
  totalSets: number
  totalPrs: number
  xpGained: number
  level: number
  currentStreak: number
  newBadges: { title: string; icon: string }[]
  exerciseNames: string[]
}

interface BadgeShareData {
  title: string
  description: string
  icon: string
  category: string
  unlockedAt: Date
}

interface PRShareData {
  exerciseName: string
  weight: number
  reps: number
  estimated1RM: number
  date: Date
}

// Fonctions
export function generateWorkoutShareText(data: WorkoutShareData, t: TranslationFn): string
export function generateBadgeShareText(data: BadgeShareData, t: TranslationFn): string
export function generatePRShareText(data: PRShareData, t: TranslationFn): string
export async function shareText(text: string): Promise<void>
export async function shareImage(viewShotRef: RefObject<ViewShot>): Promise<void>
```

**Format texte workout :**
```
🏋️ Séance Kore terminée !
⏱️ 45min | 📊 8 500 kg | 24 séries
⭐ +95 XP | Niveau 15 | 🔥 8 sem. de streak
🥇 2 PRs battus
🏅 Badge débloqué : Régulier

#Kore #Musculation
```

**Format texte badge :**
```
🏅 Badge débloqué sur Kore !
[Icon] [Titre]
[Description]

#Kore #Badge
```

**Format texte PR :**
```
🥇 Nouveau record personnel sur Kore !
[Exercice] : [Poids] kg × [Reps] reps
📈 1RM estimé : [1RM] kg

#Kore #PR #Musculation
```

### 3. `shareText()` — Partage texte simple
- Créer un fichier temp `.txt` avec `expo-file-system`
- Appeler `Sharing.shareAsync(fileUri)`
- OU utiliser `Share` de react-native (`Share.share({ message: text })`) qui est plus simple pour du texte pur

### 4. `shareImage()` — Partage image
- Prend une ref ViewShot
- Appelle `viewShotRef.current.capture()` → URI locale
- Appelle `Sharing.shareAsync(uri, { mimeType: 'image/png' })`

### 5. Ajouter les clés i18n

Dans `fr.ts`, section `share` :
```typescript
share: {
  workoutTitle: 'Séance Kore terminée !',
  duration: 'Durée',
  volume: 'Volume',
  sets: 'séries',
  prsBeaten: 'PRs battus',
  badgeUnlocked: 'Badge débloqué',
  badgeTitle: 'Badge débloqué sur Kore !',
  prTitle: 'Nouveau record personnel sur Kore !',
  estimated1RM: '1RM estimé',
  hashtags: '#Kore #Musculation',
  shareButton: 'Partager',
  shareAsImage: 'Partager en image',
  shareAsText: 'Partager en texte',
  streakWeeks: 'sem. de streak',
}
```

Idem dans `en.ts` avec les traductions anglaises.

## Contraintes
- Ne pas casser le flow d'export existant dans `SettingsDataSection.tsx`
- Ne pas modifier `exportHelpers.ts`
- Utiliser `Share` de react-native pour le texte (plus simple et natif)
- `Sharing.shareAsync` (expo-sharing) pour les fichiers/images
- Pas de `any` TypeScript
- Pas de `console.log` sans `__DEV__`
- Formater les nombres avec séparateur de milliers (ex: `8 500 kg`)

## Critères de validation
- `npx tsc --noEmit` → zéro erreur
- `npm test` → zéro fail
- Le service exporte les 5 fonctions typées
- Les clés i18n sont présentes dans fr.ts ET en.ts
- `react-native-view-shot` est dans package.json

## Dépendances
Aucune dépendance — ce groupe peut être lancé immédiatement.

## Statut
⏳ En attente
