# Tache D — Nettoyage dette technique — 20260319-1800

## Objectif
Nettoyer la section dette technique du ROADMAP (issues resolues mais encore listees), mettre a jour HEALTH.md, et auditer les `console.warn`/`console.error` sans guard `__DEV__`.

## Fichiers a modifier
- `docs/ROADMAP.md` — section "Dette technique"
- `docs/bmad/verrif/HEALTH.md` — mettre a jour le score
- Fichiers avec `console.warn`/`console.error` sans `__DEV__` (a identifier)

## Contexte technique

### Etat actuel de la dette (ROADMAP.md)
La section "Dette technique" liste 8 issues (A1, A2, B1, B2, C1, D1, D2, D3) du verrif 20260319-1408. Voici leur statut reel :

| Issue | Statut | Raison |
|-------|--------|--------|
| A1 handleGo useCallback | ✅ CORRIGE | commit a088050 |
| A2 shortcuts.map inline | ✅ CORRIGE | commit a088050 |
| B1 BackHandler stale closure | ✅ CORRIGE | commit a088050 |
| B2 handleConfirmEnd deps | ✅ CORRIGE | commit a088050 |
| C1 CoachMarks timer | ✅ FAUX POSITIF | cleanup correct (verifie manuellement) |
| D1 s.history.id | ✅ FAUX POSITIF | getter WatermelonDB sync, pas de fetch async |
| D2 render functions instables | ✅ FAUX POSITIF | pattern correct pour carousel max 5 items |
| D3 fallback 'intermediate' | ✅ FAUX POSITIF | intentionnel et type-safe |

### Actions a faire

**1. ROADMAP.md — Mettre a jour la section "Dette technique" :**
- Remplacer le tableau des 8 issues par un message indiquant que toutes sont resolues/classees
- Garder une mention du commit de correction et de la date
- La section "Phase 2 — Polish (en cours)" doit aussi etre mise a jour :
  - `[x] HomeScreen refactor (2082 lignes → split composants)` → FAIT (HomeHeroAction, HomeStatusStrip extraits)
  - `[x] DRY getMondayOfWeek (5 implementations → 1)` → FAIT (commit b8dcb76)
  - Les autres items restent `[ ]`

**2. HEALTH.md — Mettre a jour le score :**
- Lire le fichier actuel `docs/bmad/verrif/HEALTH.md`
- Ajouter une entree pour la date du jour (2026-03-19 apres corrections)
- Score attendu : 100/100 (toutes les issues sont resolues ou classees faux positif)
- Format : suivre le format existant dans le fichier

**3. Audit console.warn/console.error :**
- Chercher tous les `console.warn` et `console.error` dans `mobile/src/`
- Verifier que chacun est garde par `if (__DEV__)` ou `__DEV__ &&`
- Si un `console.warn`/`console.error` n'a pas de guard `__DEV__` → l'ajouter
- Pattern correct : `if (__DEV__) console.warn('...')` ou `if (__DEV__) console.error('...')`
- NE PAS supprimer les logs, juste les garder avec `__DEV__`

**4. Verifier console.log :**
- Chercher tous les `console.log` dans `mobile/src/`
- Il ne doit y en avoir AUCUN (meme avec `__DEV__`)
- Si trouves → supprimer ou convertir en `console.warn` avec guard `__DEV__`

## Etapes
1. Lire `docs/ROADMAP.md` et `docs/bmad/verrif/HEALTH.md`
2. Mettre a jour la section dette technique du ROADMAP
3. Mettre a jour les checkboxes Phase 2
4. Ajouter entree HEALTH.md
5. `grep -r "console\." mobile/src/` pour trouver tous les logs
6. Corriger les logs sans guard `__DEV__`
7. Verifier : `npx tsc --noEmit` → 0 erreur
8. Verifier : `npm test` → 2231+ passed

## Contraintes
- NE PAS modifier de fichiers dans `mobile/src/screens/` ou `mobile/src/components/` (sauf pour les console guards)
- NE PAS modifier la logique metier
- NE PAS supprimer les console.warn/error utiles — juste ajouter `__DEV__`
- Garder le format existant de HEALTH.md et ROADMAP.md

## Criteres de validation
- `npx tsc --noEmit` → 0 erreur
- `npm test` → 2231+ passed
- Zero `console.log` dans mobile/src/
- Zero `console.warn`/`console.error` sans guard `__DEV__` dans mobile/src/
- ROADMAP.md dette technique a jour
- HEALTH.md score mis a jour

## Dependances
Aucune — ce groupe est independant.

## Statut
⏳ En attente
