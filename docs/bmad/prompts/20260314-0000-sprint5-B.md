<!-- v1.0 — 2026-03-14 -->
# Rapport — Titres Débloquables (#38) — Groupe B — 20260314-0000

## Objectif
Créer un écran `TitlesScreen` qui liste ~15 titres narratifs débloquables (ex: "Le Squatteur", "La Bête", "Le Régulier"). Chaque titre est débloqué automatiquement selon les données existantes. Accessible depuis StatsScreen (nouveau bouton dans STAT_BUTTONS). L'utilisateur voit ses titres débloqués en couleur et les verrouillés en gris.

## Fichiers concernés
- `mobile/src/model/utils/titlesHelpers.ts` — NOUVEAU : définitions + logique de déblocage
- `mobile/src/screens/TitlesScreen.tsx` — NOUVEAU : écran principal
- `mobile/src/navigation/index.tsx` — MODIFIER : ajouter route `Titles`
- `mobile/src/screens/StatsScreen.tsx` — MODIFIER : ajouter bouton dans STAT_BUTTONS
- `mobile/src/i18n/fr.ts` — MODIFIER : ajouter clés `titles.*`
- `mobile/src/i18n/en.ts` — MODIFIER : même structure

## Contexte technique

### Stack & Architecture
- React Native + Expo 52 + TypeScript strict (Fabric / New Architecture)
- WatermelonDB (offline-first) — données via `withObservables`
- `useColors()` → `ThemeColors` depuis `mobile/src/contexts/ThemeContext.tsx`
- `useLanguage()` → `{ t }` depuis `mobile/src/contexts/LanguageContext.tsx`
- `useDeferredMount()` depuis `mobile/src/hooks/useDeferredMount.ts`
- `withObservables` + `observeCurrentUser()` depuis `mobile/src/model/utils/databaseHelpers.ts`

### Modèles disponibles
```ts
// User — table 'users'
user.totalPrs: number
user.totalTonnage: number
user.currentStreak: number
user.bestStreak: number
user.level: number

// History — table 'histories'
history.deletedAt: Date | null
history.isAbandoned: boolean

// Set — table 'sets' (pour compter les exercices distincts)
set.exercise: Relation<Exercise>
```

## Implémentation

### titlesHelpers.ts

```ts
export interface TitleDefinition {
  id: string
  name: string              // clé i18n : t.titles.names[id]
  description: string       // clé i18n : t.titles.descriptions[id]
  icon: string              // Ionicons name
  unlocked: boolean
}

/**
 * Calcule la liste des titres débloqués/verrouillés.
 * Utilise les données user + totalHistories + distinctExercises.
 */
export function computeTitles(
  user: User,
  totalHistories: number,
  distinctExercises: number,
): TitleDefinition[]
```

**15 titres définis (id → condition de déblocage) :**

| id | Condition |
|----|-----------|
| `first_steps` | totalHistories >= 1 |
| `regular` | user.bestStreak >= 4 (4 semaines) |
| `iron_will` | user.bestStreak >= 12 |
| `veteran` | totalHistories >= 50 |
| `centurion` | totalHistories >= 100 |
| `pr_hunter` | user.totalPrs >= 10 |
| `record_breaker` | user.totalPrs >= 50 |
| `tonnage_lifter` | user.totalTonnage >= 10000 |
| `tonnage_master` | user.totalTonnage >= 100000 |
| `explorer` | distinctExercises >= 20 |
| `exercise_master` | distinctExercises >= 40 |
| `level_10` | user.level >= 10 |
| `level_25` | user.level >= 25 |
| `level_50` | user.level >= 50 |
| `elite` | user.level >= 75 |

### TitlesScreen.tsx — UI

```
ScrollView :
  Header card :
    "X titres débloqués sur 15"
    Barre de progression

  Section "Débloqués" :
    Liste des titres unlocked (couleur, icon Ionicons)
    ┌─────────────────────────────────────────────────┐
    │ 🏆  La Bête                                     │
    │     100 000 kg au compteur                      │
    └─────────────────────────────────────────────────┘

  Section "Verrouillés" :
    Liste des titres locked (gris, 🔒)
    ┌─────────────────────────────────────────────────┐
    │ 🔒  L'Élite           (gris)                    │
    │     Atteindre le niveau 75                      │
    └─────────────────────────────────────────────────┘
```

**Détails UI :**
- Titres unlocked : `colors.text` (name), `colors.textSecondary` (description), icon `colors.primary`
- Titres locked : `colors.textSecondary` (name), `colors.textMuted` (description), icon `🔒` `colors.textMuted`
- Cards : `backgroundColor: colors.card`, `borderRadius: borderRadius.md`, `padding: spacing.md`

### withObservables pattern
```ts
const enhance = withObservables([], () => ({
  user: observeCurrentUser(),
  sets: database.get<WorkoutSet>('sets').query(
    Q.on('histories', Q.and(
      Q.where('deleted_at', null),
      Q.or(Q.where('is_abandoned', null), Q.where('is_abandoned', false)),
    ))
  ).observe(),
  histories: database.get<History>('histories').query(
    Q.where('deleted_at', null),
    Q.or(Q.where('is_abandoned', null), Q.where('is_abandoned', false)),
  ).observe(),
}))
```

**Calculs dans useMemo :**
```ts
const distinctExercises = useMemo(() => {
  const ids = new Set(sets.map(s => s.exercise?.id).filter(Boolean))
  return ids.size
}, [sets])

const titles = useMemo(
  () => computeTitles(user, histories.length, distinctExercises),
  [user, histories, distinctExercises],
)

const unlockedTitles = useMemo(() => titles.filter(t => t.unlocked), [titles])
const lockedTitles   = useMemo(() => titles.filter(t => !t.unlocked), [titles])
```

### Navigation — modifier navigation/index.tsx
**Lazy import** (après `StatsHallOfFameScreen`) :
```ts
const TitlesScreen = React.lazy(() => import('../screens/TitlesScreen'))
```

**RootStackParamList** (après `StatsHallOfFame: undefined`) :
```ts
Titles: undefined;
```

**Stack.Screen** (après la ligne StatsHallOfFame) :
```tsx
<Stack.Screen name="Titles" component={TitlesScreen} options={{ title: t.navigation.titles }} />
```

### StatsScreen — ajouter bouton dans STAT_BUTTONS
```ts
{ icon: 'ribbon-outline', label: t.stats.titles, route: 'Titles' },
```
Ajouter **en dernier** dans le tableau `STAT_BUTTONS`.

### Traductions

**fr.ts — dans `stats: {`** (après `hallOfFame: 'Hall of Fame'`) :
```ts
titles: 'Titres',
```

**fr.ts — dans `navigation: {`** (après `hallOfFame: 'Hall of Fame'`) :
```ts
titles: 'Mes Titres',
```

**fr.ts — nouvelle section (avant `share: {`) :**
```ts
titles: {
  title: 'Titres',
  subtitle: 'Tes titres débloqués',
  unlockedSection: 'Débloqués',
  lockedSection: 'Verrouillés',
  progress: 'titres débloqués sur',
  names: {
    first_steps: 'Premiers Pas',
    regular: 'Le Régulier',
    iron_will: 'Volonté de Fer',
    veteran: 'Le Vétéran',
    centurion: 'Le Centurion',
    pr_hunter: 'Chasseur de Records',
    record_breaker: 'Briseur de Records',
    tonnage_lifter: 'Le Tonnage',
    tonnage_master: 'Maître du Tonnage',
    explorer: 'L\'Explorateur',
    exercise_master: 'Maître des Exercices',
    level_10: 'Initié',
    level_25: 'Confirmé',
    level_50: 'Expert',
    elite: 'L\'Élite',
  },
  descriptions: {
    first_steps: 'Première séance complète',
    regular: '4 semaines de streak consécutives',
    iron_will: '12 semaines de streak consécutives',
    veteran: '50 séances au compteur',
    centurion: '100 séances au compteur',
    pr_hunter: '10 records personnels',
    record_breaker: '50 records personnels',
    tonnage_lifter: '10 000 kg de tonnage total',
    tonnage_master: '100 000 kg de tonnage total',
    explorer: '20 exercices différents pratiqués',
    exercise_master: '40 exercices différents pratiqués',
    level_10: 'Atteindre le niveau 10',
    level_25: 'Atteindre le niveau 25',
    level_50: 'Atteindre le niveau 50',
    elite: 'Atteindre le niveau 75',
  },
},
```

**en.ts — mêmes clés :**
```ts
titles: {
  title: 'Titles',
  subtitle: 'Your earned titles',
  unlockedSection: 'Unlocked',
  lockedSection: 'Locked',
  progress: 'titles unlocked out of',
  names: {
    first_steps: 'First Steps',
    regular: 'The Regular',
    iron_will: 'Iron Will',
    veteran: 'The Veteran',
    centurion: 'The Centurion',
    pr_hunter: 'PR Hunter',
    record_breaker: 'Record Breaker',
    tonnage_lifter: 'The Tonnage',
    tonnage_master: 'Tonnage Master',
    explorer: 'The Explorer',
    exercise_master: 'Exercise Master',
    level_10: 'Initiate',
    level_25: 'Confirmed',
    level_50: 'Expert',
    elite: 'The Elite',
  },
  descriptions: {
    first_steps: 'First completed session',
    regular: '4 consecutive weeks streak',
    iron_will: '12 consecutive weeks streak',
    veteran: '50 sessions completed',
    centurion: '100 sessions completed',
    pr_hunter: '10 personal records',
    record_breaker: '50 personal records',
    tonnage_lifter: '10,000 kg total tonnage',
    tonnage_master: '100,000 kg total tonnage',
    explorer: '20 different exercises tried',
    exercise_master: '40 different exercises tried',
    level_10: 'Reach level 10',
    level_25: 'Reach level 25',
    level_50: 'Reach level 50',
    elite: 'Reach level 75',
  },
},
```

## Contraintes
- Pas de migration DB — uniquement données existantes
- Pas de dépendances npm supplémentaires
- `useDeferredMount()` comme wrapper du composant observable
- Respecter `spacing.*`, `borderRadius.*`, `fontSize.*`
- Ne pas utiliser `<Modal>` natif — pas besoin ici (pas de modal)

## Critères de validation
- `npx tsc --noEmit` → zéro erreur TypeScript
- Le bouton "Titres" apparaît dans StatsScreen
- Navigation vers TitlesScreen fonctionne
- Les titres unlocked sont colorés, locked sont gris
- Barre de progression reflète le nombre de titres débloqués

## Dépendances
Aucune dépendance sur les autres groupes.

## Git — commit après implémentation
```bash
git add mobile/src/model/utils/titlesHelpers.ts \
        mobile/src/screens/TitlesScreen.tsx \
        mobile/src/navigation/index.tsx \
        mobile/src/screens/StatsScreen.tsx \
        mobile/src/i18n/fr.ts \
        mobile/src/i18n/en.ts \
        docs/bmad/do/[rapport]
git commit -m "feat(gamification): unlockable titles screen — 15 narrative titles based on progress"
git push origin develop
# Si remote ahead : git pull --rebase puis re-push
```

## Statut
⏳ En attente
