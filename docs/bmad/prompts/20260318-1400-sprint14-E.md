<!-- v1.0 — 2026-03-18 -->
# Rapport — Suggestions de Repos (#120) — Groupe E — 20260318-1400

## Objectif
Ajouter une carte "Suggestion repos" sur `HomeScreen` qui recommande intelligemment quand prendre un jour de repos. Analyse le pattern d'entrainement, la fatigue accumulee, et les muscles sollicites pour proposer un conseil personnalise.

## Fichiers concernes
- `mobile/src/model/utils/restDaySuggestionsHelpers.ts` — NOUVEAU
- `mobile/src/screens/HomeScreen.tsx` — MODIFIER : ajouter carte suggestions
- `mobile/src/i18n/fr.ts` — MODIFIER : ajouter cles `home.restSuggestion.*`
- `mobile/src/i18n/en.ts` — MODIFIER : meme structure

## Contexte technique
- React Native + Expo 52 + TypeScript strict
- `useColors()` / `useLanguage()` hooks
- HomeScreen a deja `histories`, `sets`, `exercises` dans ses props
- La carte va **apres** les milestones (Sprint 14-C) ou apres les autres cartes existantes
- Ne pas modifier withObservables
- Peut reutiliser `computeFatigueIndex` et `computeMuscleRecovery`

## Implementation

### restDaySuggestionsHelpers.ts

```ts
export interface RestSuggestion {
  shouldRest: boolean
  confidence: 'high' | 'medium' | 'low'
  reason: string            // cle i18n
  consecutiveDays: number   // jours d'entrainement consecutifs
  musclesTired: string[]    // muscles sous 50% recuperation
  fatigueLevel: 'low' | 'moderate' | 'high' | 'critical'
  suggestion: string        // cle i18n pour la suggestion
}

/**
 * Determine si un jour de repos est recommande.
 *
 * @param histories — seances non-abandonnees
 * @param sets — tous les sets
 * @param exercises — tous les exercices
 *
 * Algorithme :
 * 1. Compter les jours d'entrainement consecutifs (sans repos)
 * 2. Calculer la fatigue via computeFatigueIndex (ACWR)
 * 3. Lister les muscles sous 50% de recuperation via computeMuscleRecovery
 * 4. Decision :
 *    - consecutiveDays >= 5 → shouldRest = true, confidence = 'high', reason = 'tooManyDays'
 *    - ACWR > 1.5 → shouldRest = true, confidence = 'high', reason = 'highFatigue'
 *    - ACWR > 1.3 ET consecutiveDays >= 3 → shouldRest = true, confidence = 'medium', reason = 'accumulatedFatigue'
 *    - musclesTired.length > 3 → shouldRest = true, confidence = 'medium', reason = 'muscleRecovery'
 *    - consecutiveDays >= 3 ET musclesTired.length > 1 → shouldRest = true, confidence = 'low', reason = 'mildFatigue'
 *    - sinon → shouldRest = false
 * 5. Suggestions specifiques :
 *    - shouldRest + high → 'takeFullRest'
 *    - shouldRest + medium → 'lightActivity'
 *    - shouldRest + low → 'optionalRest'
 *    - !shouldRest → 'readyToTrain'
 */
export function computeRestSuggestion(
  histories: Array<{ startedAt: Date | number; isAbandoned: boolean }>,
  sets: Array<{ weight: number; reps: number; exerciseId: string; createdAt: Date | number }>,
  exercises: Array<{ id: string; muscles: string[] }>,
): RestSuggestion
```

### Section UI dans HomeScreen

```tsx
{restSuggestion && restSuggestion.shouldRest && (
  <View style={[styles.restCard, {
    borderLeftColor: restSuggestion.confidence === 'high' ? colors.danger
      : restSuggestion.confidence === 'medium' ? '#F59E0B' : colors.textSecondary,
  }]}>
    <View style={styles.restHeader}>
      <Text style={styles.restTitle}>{t.home.restSuggestion.title}</Text>
      <View style={[styles.restConfidenceBadge, {
        backgroundColor: restSuggestion.confidence === 'high' ? colors.danger
          : restSuggestion.confidence === 'medium' ? '#F59E0B' : colors.textSecondary,
      }]}>
        <Text style={styles.restConfidenceText}>
          {t.home.restSuggestion.confidence[restSuggestion.confidence]}
        </Text>
      </View>
    </View>
    <Text style={styles.restReason}>
      {t.home.restSuggestion.reasons[restSuggestion.reason]}
    </Text>
    <Text style={styles.restSuggestion}>
      {t.home.restSuggestion.suggestions[restSuggestion.suggestion]}
    </Text>
    {restSuggestion.musclesTired.length > 0 && (
      <Text style={styles.restMuscles}>
        {t.home.restSuggestion.tiredMuscles}: {restSuggestion.musclesTired.join(', ')}
      </Text>
    )}
  </View>
)}
```

**Styles :**
```ts
restCard: {
  marginHorizontal: spacing.md, marginTop: spacing.md,
  backgroundColor: colors.card, borderRadius: borderRadius.lg, padding: spacing.md,
  borderLeftWidth: 4,
},
restHeader: {
  flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: spacing.sm,
},
restTitle: { fontSize: fontSize.sm, fontWeight: '700', color: colors.text },
restConfidenceBadge: {
  paddingHorizontal: spacing.sm, paddingVertical: 2, borderRadius: borderRadius.xs,
},
restConfidenceText: { fontSize: fontSize.caption, fontWeight: '600', color: '#fff' },
restReason: { fontSize: fontSize.bodyMd, color: colors.text, marginBottom: spacing.xs },
restSuggestion: {
  fontSize: fontSize.caption, color: colors.textSecondary, fontStyle: 'italic',
  marginBottom: spacing.xs,
},
restMuscles: { fontSize: fontSize.caption, color: colors.textSecondary },
```

### Traductions

**fr.ts — dans `home: {`** :
```ts
restSuggestion: {
  title: 'Suggestion de repos',
  confidence: {
    high: 'Fortement recommande',
    medium: 'Recommande',
    low: 'Optionnel',
  },
  reasons: {
    tooManyDays: 'Tu t\'entraines depuis 5 jours consecutifs.',
    highFatigue: 'Ton indice de fatigue est trop eleve.',
    accumulatedFatigue: 'Fatigue accumulee detectee.',
    muscleRecovery: 'Plusieurs muscles n\'ont pas recupere.',
    mildFatigue: 'Legere fatigue accumulee.',
  },
  suggestions: {
    takeFullRest: 'Prends une journee de repos complet.',
    lightActivity: 'Privilegie une activite legere (marche, etirements).',
    optionalRest: 'Un jour de repos serait benefique mais pas obligatoire.',
    readyToTrain: 'Tu es pret a t\'entrainer !',
  },
  tiredMuscles: 'Muscles fatigues',
},
```

**en.ts :**
```ts
restSuggestion: {
  title: 'Rest suggestion',
  confidence: {
    high: 'Strongly recommended',
    medium: 'Recommended',
    low: 'Optional',
  },
  reasons: {
    tooManyDays: 'You\'ve been training for 5 consecutive days.',
    highFatigue: 'Your fatigue index is too high.',
    accumulatedFatigue: 'Accumulated fatigue detected.',
    muscleRecovery: 'Several muscles haven\'t recovered.',
    mildFatigue: 'Mild accumulated fatigue.',
  },
  suggestions: {
    takeFullRest: 'Take a full rest day.',
    lightActivity: 'Prefer light activity (walk, stretching).',
    optionalRest: 'A rest day would help but is not mandatory.',
    readyToTrain: 'You\'re ready to train!',
  },
  tiredMuscles: 'Tired muscles',
},
```

## Contraintes
- Ephemere — pas de DB
- Reutiliser les helpers existants (fatigueIndex, muscleRecovery) — import, pas de copie
- Ne PAS ajouter `as const` dans en.ts
- Utiliser `textSecondary` (pas textMuted)
- Ne pas modifier withObservables
- La carte ne s'affiche QUE si shouldRest = true

## Criteres de validation
- `npx tsc --noEmit` → zero erreur
- Carte visible sur HomeScreen uniquement quand repos recommande
- Badge de confiance colore
- Raison et suggestion affichees
- Muscles fatigues listes si applicable

## Git
```bash
git add mobile/src/model/utils/restDaySuggestionsHelpers.ts \
        mobile/src/screens/HomeScreen.tsx \
        mobile/src/i18n/fr.ts \
        mobile/src/i18n/en.ts
git commit -m "feat(home): rest day suggestions — smart recovery recommendations based on fatigue analysis"
git push origin develop
```

## Statut
✅ Résolu — 20260318-1400

## Résolution
Rapport do : docs/bmad/do/20260318-1400-feat-rest-suggestions.md
