<!-- v1.0 — 2026-02-26 -->
# Rapport — ui-polish — Groupe C (Icons Composants & Écrans secondaires) — 20260226-2200

## Objectif
Remplacer TOUS les emojis utilisés comme icônes dans les composants et écrans secondaires par des icônes
Ionicons. Même objectif que le Groupe B : design épuré et professionnel.

## Bibliothèque d'icônes
`@expo/vector-icons` est déjà inclus dans Expo — **aucune installation supplémentaire**.

Import à utiliser :
```tsx
import { Ionicons } from '@expo/vector-icons'
```

## Carte d'icônes commune (identique au Groupe B — à respecter pour cohérence)
| Concept | Ionicons name |
|---------|--------------|
| Supprimer/Corbeille | `trash-outline` |
| Modifier/Crayon | `pencil-outline` |
| Dupliquer | `copy-outline` |
| Valider/Check | `checkmark-outline` |
| Records/Tendance | `trending-up-outline` |
| Tendance hausse | `chevron-up-outline` |
| Tendance baisse | `chevron-down-outline` |
| Volume/Haltère | `barbell-outline` |
| Sets/Validées | `checkmark-circle-outline` |
| Records/Trophée | `trophy-outline` |
| Première fois/Confettis | `star-outline` |
| Recherche | `search-outline` |
| Streak/Flamme | `flame-outline` |
| Avertissement | `warning-outline` |
| Créer/Plus | `add-circle-outline` |
| Dossier/Programme | `folder-open-outline` |

## Fichiers concernés
1. `mobile/src/components/WorkoutSummarySheet.tsx`
2. `mobile/src/screens/ProgramsScreen.tsx`
3. `mobile/src/screens/ExercisesScreen.tsx`
4. `mobile/src/screens/ChartsScreen.tsx`
5. `mobile/src/screens/StatsMeasurementsScreen.tsx`
6. `mobile/src/components/SessionExerciseItem.tsx`
7. `mobile/src/components/WorkoutExerciseCard.tsx`

## Transformations fichier par fichier

### WorkoutSummarySheet.tsx
Lignes ~51-56 : Textes avec emojis dans `getPerformanceLabel()` :
- `'🏅 Record battu !'` → `'Record battu !'` (le badge sera un Ionicons dans le rendu)
- `'🔺 En progression !'` → `'En progression !'`
- `'💪 Bonne séance !'` → `'Bonne séance !'`

Lignes ~144-146 : `StatBlock` avec prop `emoji` :
- `emoji="🏋️"` → remplacer par `icon="barbell-outline"` (changer le type de prop)
- `emoji="✅"` → `icon="checkmark-circle-outline"`
- `emoji="🏆"` → `icon="trophy-outline"`

Dans le composant `StatBlock` interne : remplacer `<Text>{emoji}</Text>` par `<Ionicons name={icon} size={20} color={colors.primary} />`

Ligne ~177 : `<Text style={styles.completeBadge}>✅</Text>` → `<Ionicons name="checkmark-circle" size={18} color={colors.success} />`

Ligne ~200 : `<Text style={styles.progressionFirstTime}>🎉 Première séance !</Text>` → retirer l'emoji, garder juste `Première séance !` (ou ajouter une `star-outline` en Ionicons)

Lignes ~206, ~210, ~229 : emojis `🔺` et `🔻` pour tendances → remplacer par Ionicons inline :
```tsx
<Ionicons name="chevron-up-outline" size={12} color={colors.success} />
<Ionicons name="chevron-down-outline" size={12} color={colors.danger} />
```

**Mise à jour tests** : `WorkoutSummarySheet.test.tsx` cherche `'🏋️ 2500.5 kg'`, `'✅ 12 validées'`, `'🏆 3 PR'` → les tests devront chercher `'2500.5 kg'`, `'12 validées'`, `'3 PR'`.

### ProgramsScreen.tsx
Ligne ~261 : `📂 Créer un Programme` → remplacer par :
```tsx
<View style={{ flexDirection: 'row', alignItems: 'center', gap: 8 }}>
  <Ionicons name="add-circle-outline" size={20} color={colors.text} />
  <Text style={styles.btnText}>Créer un Programme</Text>
</View>
```

Lignes ~283, ~294 : `✏️` et `✨` dans sheet options → Ionicons dans les `sheetOptionIcon` :
- `✏️` → `<Ionicons name="pencil-outline" size={20} color={colors.text} />`
- `✨` → `<Ionicons name="hardware-chip-outline" size={20} color={colors.primary} />` (option IA)

Lignes ~347-382 : Sheet options (renommer, dupliquer, supprimer) :
- `✏️` → `<Ionicons name="pencil-outline" size={20} color={colors.text} />`
- `👯` → `<Ionicons name="copy-outline" size={20} color={colors.text} />`
- `🗑️` → `<Ionicons name="trash-outline" size={20} color={colors.danger} />`

Supprimer le style `sheetOptionIcon` s'il n'est utilisé que pour les emojis text.

### ExercisesScreen.tsx
Ligne ~181 : `🔍 Rechercher un exercice...` → champ de recherche avec icône :
Remplacer le texte fakefake par un vrai `<TextInput>` avec icône Ionicons à gauche (ou garder le fake mais avec `<Ionicons name="search-outline" size={16} color={colors.placeholder} />` à la place de l'emoji).

Ligne ~252 : `✏️` → `<Ionicons name="pencil-outline" size={20} color={colors.text} />`
Ligne ~255 : `🗑️` → `<Ionicons name="trash-outline" size={20} color={colors.danger} />`

### ChartsScreen.tsx
Ligne ~132 : `<Text style={styles.deleteIcon}>🗑️</Text>` → `<Ionicons name="trash-outline" size={20} color={colors.danger} />`

**Mise à jour tests** : `ChartsScreen.test.tsx` cherche `getAllByText('🗑️')` à plusieurs endroits → remplacer par `getAllByTestId('delete-btn')` ou ajouter `testID="delete-btn"` sur le TouchableOpacity parent, puis adapter les tests.

### StatsMeasurementsScreen.tsx
Ligne ~233 : `<Text style={styles.deleteIcon}>🗑️</Text>` → `<Ionicons name="trash-outline" size={20} color={colors.danger} />`

**Mise à jour tests** : `StatsMeasurementsScreen.test.tsx` cherche `getByText('🗑️')` → remplacer par `testID` sur le bouton.

### SessionExerciseItem.tsx
Ligne ~71 : `<Text style={styles.deleteIcon}>🗑️</Text>` → `<Ionicons name="trash-outline" size={20} color={colors.danger} />`

**Mise à jour tests** : `SessionExerciseItem.test.tsx` cherche `getByText('🗑️')` → adapter.

### WorkoutExerciseCard.tsx
Lignes ~115, ~165 : `<Text style={styles.validateBtnText}>✓</Text>` → `<Ionicons name="checkmark-outline" size={18} color="white" />`

Note : le ✓ actuel est un caractère unicode (U+2713), pas un emoji — mais le remplacer par Ionicons améliore quand même l'apparence.

**Mise à jour tests** : `WorkoutExerciseCard.test.tsx` cherche `getByText('✓')` à plusieurs endroits → adapter pour utiliser `testID` ou `getByLabelText`.

## Stratégie pour les tests de delete/validate
Pour les éléments supprimés (🗑️, ✓), ajouter `testID` sur le `TouchableOpacity` parent :
- Bouton delete : `testID="delete-btn"`
- Bouton validate : `testID="validate-btn"`

Puis dans les tests :
```ts
// Avant
fireEvent.press(getByText('🗑️'))
// Après
fireEvent.press(getByTestId('delete-btn'))
```

## Contraintes
- `useColors()` pour toutes les couleurs — jamais hardcodé
- `@expo/vector-icons` est déjà disponible — ne pas installer d'autre lib
- Taille icônes : 20px pour les actions inline, 18px pour les petites icônes, 16px pour les badges
- Couleur icônes delete : `colors.danger`, icônes edit/action neutres : `colors.text` ou `colors.textSecondary`
- Mettre à jour les tests impactés pour utiliser `testID` au lieu de `getByText(emoji)`

## Critères de validation
- `npx tsc --noEmit` → zéro erreur
- `npm test` → zéro fail (adapter les tests si nécessaire)
- Aucun emoji visible dans ces 7 fichiers
- Les icônes Ionicons s'affichent correctement

## Dépendances
Aucune — peut tourner en parallèle avec Groupe A et Groupe B.

## Statut
✅ Résolu — 20260226-2200

## Résolution
Rapport do : docs/bmad/do/20260226-2200-style-icons-groupe-c.md
