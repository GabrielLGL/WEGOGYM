<!-- v1.0 — 2026-03-14 -->
# Prompt — Sprint 5 (5 features parallèles) — 20260314-0000

## Demande originale
"Les 5 claudes ont finit, vérifie et mets à jour les ajouts puis donne-moi 5 autres tâches pour eux"

## Vérification Sprint 4
- ✅ `npx tsc --noEmit` → 0 erreur TypeScript
- ✅ Tous les fichiers créés : `StatsHallOfFameScreen.tsx`, `StatsHexagonScreen.tsx`, `HexagonStatsCard.tsx`, `ActivityFeedScreen.tsx`, `flashbackHelpers.ts`, `prPredictionHelpers.ts`
- ✅ Index Sprint 4 mis à jour — tous les groupes ✅

## Groupes générés

| Groupe | Rapport | Feature | Fichiers touchés | Vague | Statut |
|--------|---------|---------|-----------------|-------|--------|
| A | `20260314-0000-sprint5-A.md` | #36 Classe athlète | athleteClassHelpers + HomeScreen + i18n | 1 | ⏳ |
| B | `20260314-0000-sprint5-B.md` | #38 Titres débloquables | titlesHelpers + TitlesScreen + navigation + StatsScreen + i18n | 1 | ⏳ |
| C | `20260314-0000-sprint5-C.md` | #29 Ratios de force / Équilibre | StatsBalanceScreen + navigation + StatsScreen + i18n | 1 | ⏳ |
| D | `20260314-0000-sprint5-D.md` | #46 Graphiques multi-axes | ExerciseHistoryScreen + i18n | 1 | ⏳ |
| E | `20260314-0000-sprint5-E.md` | #74 Exercice de la semaine | exerciseOfWeekHelpers + HomeScreen + i18n | 1 | ⏳ |

## Ordre d'exécution
**Toutes les 5 features sont en Vague 1 — lancer en PARALLÈLE.**

Aucune feature ne dépend d'une autre.

### Conflits anticipés et résolution

| Fichier | Groupes concernés | Résolution |
|---------|------------------|------------|
| `fr.ts` / `en.ts` | A, B, C, D, E | Chacun ajoute une section unique → `git pull --rebase` |
| `navigation/index.tsx` | B, C | Chacun ajoute 1 import + 1 paramType + 1 Screen → `git pull --rebase` |
| `StatsScreen.tsx` | B, C | Chacun ajoute 1 bouton unique à la fin de STAT_BUTTONS → `git pull --rebase` |
| `HomeScreen.tsx` | A (dans gamificationCard ~l.492), E (dans outils section ~l.652) | Positions éloignées → `git pull --rebase` |
| `ExerciseHistoryScreen.tsx` | D uniquement | Pas de conflit |

## Instructions pour chaque Claude

### Ce que chaque Claude DOIT faire :
1. Lire SON rapport en entier avant de commencer
2. Lire les fichiers mentionnés dans "Fichiers concernés" (pour comprendre les patterns existants)
3. Écrire le code selon les spécifications exactes du rapport
4. Lancer `npx tsc --noEmit` et corriger toute erreur TypeScript
5. Faire `git add` UNIQUEMENT sur ses propres fichiers + son rapport
6. Vérifier avec `git diff --cached --name-only` avant de commit
7. Si `git push` échoue → `git pull --rebase` puis re-push
8. Sauvegarder un rapport dans `docs/bmad/do/[YYYYMMDD-HHmm]-feat-[nom].md`

### Ce que chaque Claude NE DOIT PAS faire :
- `git add .` ou `git add -A` — INTERDIT
- Modifier des fichiers non listés dans son rapport
- Créer des migrations DB (toutes les features sont DB-free)
- Ajouter des dépendances npm (toutes les libs nécessaires sont déjà installées)

## Vérification finale (après tous les merges)
```bash
npx tsc --noEmit    # 0 erreur
npm test            # 0 fail
```
