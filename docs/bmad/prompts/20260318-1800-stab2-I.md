<!-- v1.0 — 2026-03-18 -->
# Rapport — Tests Ecrans Lot I — Stabilisation 2-I — 20260318-1800

## Objectif
Ecrire les tests de rendu pour 6 ecrans gamification/social qui n'ont pas de couverture.
Phase 1 du RAPPORT-MVP : stabilisation avant release.

## Fichiers a creer
- `mobile/src/screens/__tests__/LeaderboardScreen.test.tsx`
- `mobile/src/screens/__tests__/SelfLeaguesScreen.test.tsx`
- `mobile/src/screens/__tests__/SkillTreeScreen.test.tsx`
- `mobile/src/screens/__tests__/ActivityFeedScreen.test.tsx`
- `mobile/src/screens/__tests__/TitlesScreen.test.tsx`
- `mobile/src/screens/__tests__/PersonalChallengesScreen.test.tsx`

## Contexte technique
- Jest + React Native Testing Library (`@testing-library/react-native`)
- Chaque ecran a une version `*Base` non-wrapped par withObservables — c'est celle a tester
- Pattern : voir `StatsConstellationScreen.test.tsx` comme reference

## Pattern de test

```tsx
import React from 'react'
import { render } from '@testing-library/react-native'
import { SomeScreenBase } from '../SomeScreen'

jest.mock('expo-haptics', () => ({
  impactAsync: jest.fn(),
  notificationAsync: jest.fn(),
  ImpactFeedbackStyle: { Light: 'Light', Medium: 'Medium', Heavy: 'Heavy' },
  NotificationFeedbackType: { Success: 'Success', Error: 'Error' },
}))

jest.mock('../../model/index', () => ({
  database: { write: jest.fn(), get: jest.fn() },
}))
```

## Consignes pour chaque ecran

### Regles generales
1. **Lire le fichier source** avant d'ecrire le test
2. Importer la version `*Base`
3. Minimum 3 tests : vide, empty state, avec donnees
4. Mock `expo-haptics` et `../../model/index`
5. Si le screen utilise `@react-navigation/native` → mock navigation : `jest.mock('@react-navigation/native', () => ({ useNavigation: () => ({ navigate: jest.fn(), goBack: jest.fn() }), useRoute: () => ({ params: {} }) }))`

### LeaderboardScreen
- Rendu vide (pas de snapshots)
- Affiche le classement avec donnees
- Utilisateur courant mis en evidence

### SelfLeaguesScreen
- Rendu vide
- Ligues hebdo/mensuelle affichees
- Classement par categorie

### SkillTreeScreen
- Rendu vide (tout verrouille)
- 4 branches affichees
- Noeuds debloques en couleur
- SVG possible → mock si necessaire

### ActivityFeedScreen
- Rendu vide
- Feed avec activites recentes
- Differents types d'activites (PR, badge, seance)

### TitlesScreen
- Rendu vide
- Liste de titres
- Titre actif mis en evidence

### PersonalChallengesScreen
- Rendu vide
- Challenges avec progression
- Barre de progression visible

## Contraintes
- NE PAS modifier les ecrans
- Minimum 3 tests par ecran
- Noms de tests en francais
- Tous les tests doivent passer

## Git
```bash
git add mobile/src/screens/__tests__/LeaderboardScreen.test.tsx \
        mobile/src/screens/__tests__/SelfLeaguesScreen.test.tsx \
        mobile/src/screens/__tests__/SkillTreeScreen.test.tsx \
        mobile/src/screens/__tests__/ActivityFeedScreen.test.tsx \
        mobile/src/screens/__tests__/TitlesScreen.test.tsx \
        mobile/src/screens/__tests__/PersonalChallengesScreen.test.tsx
git commit -m "test(screens): render tests for Leaderboard, SelfLeagues, SkillTree, ActivityFeed, Titles, PersonalChallenges screens"
git push origin develop
```

## Statut
✅ Résolu — 20260319-0800

## Résolution
Rapport do : docs/bmad/do/20260319-0800-test-screens-lot-i.md
