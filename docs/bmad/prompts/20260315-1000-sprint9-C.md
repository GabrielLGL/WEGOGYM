<!-- v1.0 — 2026-03-15 -->
# Rapport — Heatmap Musculaire (#93) — Groupe C — 20260315-1000

## Objectif
Créer un écran `StatsHeatmapScreen` affichant une heatmap visuelle des groupes musculaires travaillés récemment. Chaque muscle est représenté par une carte avec une intensité de couleur proportionnelle au volume des 7/14/30 derniers jours. Accessible depuis StatsScreen.

## Fichiers concernés
- `mobile/src/model/utils/muscleHeatmapHelpers.ts` — NOUVEAU : calcul des volumes par muscle
- `mobile/src/screens/StatsHeatmapScreen.tsx` — NOUVEAU : écran principal
- `mobile/src/navigation/index.tsx` — MODIFIER : ajouter route `StatsHeatmap`
- `mobile/src/screens/StatsScreen.tsx` — MODIFIER : ajouter bouton dans STAT_BUTTONS
- `mobile/src/i18n/fr.ts` — MODIFIER : ajouter section `heatmap.*`
- `mobile/src/i18n/en.ts` — MODIFIER : même structure

## Contexte technique

### Stack & Architecture
- React Native + Expo 52 + TypeScript strict (Fabric / New Architecture)
- WatermelonDB — données via `withObservables`
- `useColors()` / `useLanguage()` / `useDeferredMount()`
- `MUSCLES_LIST` depuis `mobile/src/model/constants.ts` :
  `['Pecs', 'Dos', 'Quadriceps', 'Ischios', 'Mollets', 'Trapèzes', 'Epaules', 'Biceps', 'Triceps', 'Abdos', 'Cardio']`

### Modèles disponibles
```ts
// Exercise — table 'exercises'
exercise.muscles: string[]    // sous-array de MUSCLES_LIST

// Set — table 'sets'
set.weight: number
set.reps: number
set.exerciseId: string
set.createdAt: Date

// History — table 'histories'
history.deletedAt: Date | null
history.isAbandoned: boolean
```

## Implémentation

### muscleHeatmapHelpers.ts

```ts
export interface MuscleHeatmapEntry {
  muscle: string
  totalVolume: number       // kg (weight × reps)
  sessionCount: number      // nombre de séances où le muscle a été travaillé
  intensity: number         // 0-1 normalisé (relatif au muscle le plus travaillé)
}

export type HeatmapPeriod = 7 | 14 | 30

/**
 * Calcule le volume par groupe musculaire sur une période donnée.
 */
export function computeMuscleHeatmap(
  sets: Array<{ weight: number; reps: number; exerciseId: string; createdAt: Date | number }>,
  exercises: Array<{ id: string; muscles: string[] }>,
  periodDays: HeatmapPeriod,
): MuscleHeatmapEntry[]
```

**Algorithme :**
1. Filtrer les sets des `periodDays` derniers jours
2. Pour chaque set, trouver l'exercice correspondant → ses muscles
3. Distribuer le volume (weight × reps) à chaque muscle de l'exercice
4. Compter les jours distincts par muscle (sessionCount)
5. Normaliser `intensity` : muscle avec le plus de volume = 1.0, les autres proportionnellement
6. Retourner trié par volume décroissant

### StatsHeatmapScreen.tsx — UI

```
Header: "Heatmap Musculaire"
Toggle période: [7j] [14j] [30j]

Grille 2 colonnes de cartes muscle :
  ┌────────────────┐ ┌────────────────┐
  │ Pecs       85% │ │ Dos        72% │
  │ ██████████░░   │ │ ████████░░░░   │
  │ 4 850 kg       │ │ 3 200 kg       │
  │ 5 séances      │ │ 4 séances      │
  └────────────────┘ └────────────────┘
  ┌────────────────┐ ┌────────────────┐
  │ Quadriceps 60% │ │ Epaules    45% │
  │ ███████░░░░░   │ │ █████░░░░░░    │
  │ ...            │ │ ...            │
  └────────────────┘ └────────────────┘

  Muscles non travaillés (grisés en bas)
```

**Détails UI :**
- Grille FlatList `numColumns={2}`
- Chaque carte : fond `colors.card`, barre de progression colorée
- Couleur de la barre : interpole de `colors.textMuted` (0%) à `colors.primary` (100%)
- Volume en kg formaté, nombre de séances
- Muscles avec 0 volume → affichés en bas, grisés, avec "—"
- Toggle période : `ChipSelector` ou 3 `TouchableOpacity` boutons

### withObservables pattern
```ts
const enhance = withObservables([], () => ({
  sets: database.get<WorkoutSet>('sets').query(
    Q.on('histories', Q.and(
      Q.where('deleted_at', null),
      Q.or(Q.where('is_abandoned', null), Q.where('is_abandoned', false)),
    )),
  ).observe(),
  exercises: database.get<Exercise>('exercises').query().observe(),
}))
```

### Navigation — modifier navigation/index.tsx
**Lazy import :**
```ts
const StatsHeatmapScreen = React.lazy(() => import('../screens/StatsHeatmapScreen'))
```

**RootStackParamList :**
```ts
StatsHeatmap: undefined;
```

**Stack.Screen :**
```tsx
<Stack.Screen name="StatsHeatmap" component={StatsHeatmapScreen} options={{ title: t.navigation.statsHeatmap }} />
```

### StatsScreen — ajouter dans STAT_BUTTONS
Lire `StatsScreen.tsx` et trouver le tableau `STAT_BUTTONS`. Ajouter :
```ts
{ route: 'StatsHeatmap' as const, icon: 'flame-outline', label: t.stats.heatmap },
```

### Traductions

**fr.ts — dans `navigation: {` :**
```ts
statsHeatmap: 'Heatmap Musculaire',
```

**fr.ts — dans `stats: {` :**
```ts
heatmap: 'Heatmap',
```

**fr.ts — nouvelle section :**
```ts
heatmap: {
  title: 'Heatmap Musculaire',
  period7: '7 jours',
  period14: '14 jours',
  period30: '30 jours',
  volume: 'Volume',
  sessions: 'séances',
  noData: 'Non travaillé',
},
```

**en.ts :**
```ts
heatmap: {
  title: 'Muscle Heatmap',
  period7: '7 days',
  period14: '14 days',
  period30: '30 days',
  volume: 'Volume',
  sessions: 'sessions',
  noData: 'Not trained',
},
```

## Contraintes
- Pas de migration DB
- `useDeferredMount()` comme wrapper
- FlatList `numColumns={2}` avec `columnWrapperStyle`
- Pas de dépendances npm supplémentaires

## Critères de validation
- `npx tsc --noEmit` → zéro erreur
- Bouton "Heatmap" dans StatsScreen
- Navigation fonctionne
- 11 muscles affichés (tous les MUSCLES_LIST)
- Toggle période change les données
- Muscles non travaillés grisés en bas

## Dépendances
Aucune dépendance sur les autres groupes.

## Git
```bash
git add mobile/src/model/utils/muscleHeatmapHelpers.ts \
        mobile/src/screens/StatsHeatmapScreen.tsx \
        mobile/src/navigation/index.tsx \
        mobile/src/screens/StatsScreen.tsx \
        mobile/src/i18n/fr.ts \
        mobile/src/i18n/en.ts
git commit -m "feat(stats): muscle heatmap screen — volume intensity by muscle group with period filter"
git push origin develop
```

## Statut
✅ Résolu — 20260315-1000

## Résolution
Rapport do : docs/bmad/do/20260315-1000-feat-stats-heatmap.md
