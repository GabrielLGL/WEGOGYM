<!-- v1.0 — 2026-03-18 -->
# Rapport — Tests Ecrans Lot G — Stabilisation 2-G — 20260318-1800

## Objectif
Ecrire les tests de rendu pour 6 ecrans Stats Sprint 11-14 qui n'ont pas de couverture.
Phase 1 du RAPPORT-MVP : stabilisation avant release.

## Fichiers a creer
- `mobile/src/screens/__tests__/StatsRestTimeScreen.test.tsx`
- `mobile/src/screens/__tests__/StatsVolumeDistributionScreen.test.tsx`
- `mobile/src/screens/__tests__/StatsMonthlyProgressScreen.test.tsx`
- `mobile/src/screens/__tests__/StatsMuscleBalanceScreen.test.tsx`
- `mobile/src/screens/__tests__/StatsExerciseFrequencyScreen.test.tsx`
- `mobile/src/screens/__tests__/StatsSetQualityScreen.test.tsx`

## Contexte technique
- Jest + React Native Testing Library (`@testing-library/react-native`)
- Chaque ecran a une version `*Base` non-wrapped par withObservables — c'est celle a tester
- Pattern : `import { StatsRestTimeScreenBase } from '../StatsRestTimeScreen'`

## Pattern de test (reference : StatsConstellationScreen.test.tsx)

```tsx
import React from 'react'
import { render } from '@testing-library/react-native'
import { SomeScreenBase } from '../SomeScreen'

jest.mock('expo-haptics', () => ({
  impactAsync: jest.fn(),
  notificationAsync: jest.fn(),
  ImpactFeedbackStyle: { Light: 'Light', Medium: 'Medium', Heavy: 'Heavy' },
  NotificationFeedbackType: { Success: 'Success', Error: 'Error' },
}))

jest.mock('../../model/index', () => ({
  database: { write: jest.fn(), get: jest.fn() },
}))
```

## Consignes pour chaque ecran

### Regles generales
1. **Lire le fichier source** avant d'ecrire le test pour comprendre les props attendues
2. Importer la version `*Base` (pas le default export wrapped)
3. Tester au minimum :
   - Rendu sans crash avec props vides/minimales
   - Affichage empty state
   - Affichage avec donnees de test
4. Mock `expo-haptics` et `../../model/index` systematiquement
5. Utiliser `as never` pour caster les props mock

### StatsRestTimeScreen
- Rendu vide (pas de sets)
- Affiche les stats de temps de repos avec donnees
- Filtre par periode fonctionne

### StatsVolumeDistributionScreen
- Rendu vide
- Distribution par muscle affichee
- Pourcentages visibles

### StatsMonthlyProgressScreen
- Rendu vide
- Progression mois par mois affichee
- Delta affiche (positif/negatif)

### StatsMuscleBalanceScreen
- Rendu vide
- 4 paires musculaires affichees
- Status colore (balanced/imbalanced)

### StatsExerciseFrequencyScreen
- Rendu vide
- Liste triee par frequence
- Section negliges si applicable

### StatsSetQualityScreen
- Rendu vide
- Grades colores (A/B/C/D)
- Score global affiche

## Contraintes
- NE PAS modifier les ecrans — seulement creer les fichiers .test.tsx
- Minimum 3 tests par ecran
- Noms de tests en francais
- Tous les tests doivent passer

## Git
```bash
git add mobile/src/screens/__tests__/StatsRestTimeScreen.test.tsx \
        mobile/src/screens/__tests__/StatsVolumeDistributionScreen.test.tsx \
        mobile/src/screens/__tests__/StatsMonthlyProgressScreen.test.tsx \
        mobile/src/screens/__tests__/StatsMuscleBalanceScreen.test.tsx \
        mobile/src/screens/__tests__/StatsExerciseFrequencyScreen.test.tsx \
        mobile/src/screens/__tests__/StatsSetQualityScreen.test.tsx
git commit -m "test(screens): render tests for RestTime, VolumeDistribution, MonthlyProgress, MuscleBalance, ExerciseFrequency, SetQuality screens"
git push origin develop
```

## Statut
✅ Résolu — 20260319-0000

## Résolution
Rapport do : docs/bmad/do/20260319-0000-test-stab2-G.md
