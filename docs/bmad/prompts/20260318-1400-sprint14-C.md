<!-- v1.0 — 2026-03-18 -->
# Rapport — Milestones de Streak (#118) — Groupe C — 20260318-1400

## Objectif
Ajouter une section "Milestones" sur `HomeScreen` montrant les jalons de streak atteints et le prochain a debloquer. Jalons : 3j, 7j, 14j, 30j, 60j, 100j, 200j, 365j. Avec animation visuelle quand un jalon est atteint.

## Fichiers concernes
- `mobile/src/model/utils/streakMilestonesHelpers.ts` — NOUVEAU
- `mobile/src/screens/HomeScreen.tsx` — MODIFIER : ajouter section milestones
- `mobile/src/i18n/fr.ts` — MODIFIER : ajouter cles `home.milestones.*`
- `mobile/src/i18n/en.ts` — MODIFIER : meme structure

## Contexte technique
- React Native + Expo 52 + TypeScript strict
- `useColors()` / `useLanguage()` hooks
- HomeScreen a deja `histories` dans ses props
- La section va **apres** la carte derniere seance (Sprint 14-A) ou apres les cartes existantes
- Ne pas modifier withObservables
- Peut reutiliser le calcul de streak existant dans gamificationHelpers

## Implementation

### streakMilestonesHelpers.ts

```ts
export interface Milestone {
  days: number
  label: string          // "1 semaine", "1 mois", etc.
  icon: string           // emoji ou icone
  reached: boolean
  reachedDate: Date | null
}

export interface MilestonesResult {
  currentStreak: number
  milestones: Milestone[]
  nextMilestone: Milestone | null
  daysToNext: number
  progressToNext: number  // 0-100
}

/**
 * Calcule les milestones de streak.
 *
 * @param histories — seances non-abandonnees
 *
 * Algorithme :
 * 1. Calculer le streak actuel (jours consecutifs avec au moins 1 seance)
 *    - Un "jour" = date locale (pas 24h glissantes)
 *    - Max 1 jour de gap autorise (repos)
 *    - Compter les jours uniques d'entrainement, pas les seances
 * 2. Milestones predefinies : [3, 7, 14, 30, 60, 100, 200, 365]
 * 3. Pour chaque milestone : reached = currentStreak >= days
 * 4. nextMilestone = premier milestone non-atteint
 * 5. daysToNext = nextMilestone.days - currentStreak
 * 6. progressToNext = (currentStreak - lastReached.days) / (nextMilestone.days - lastReached.days) * 100
 */
export function computeStreakMilestones(
  histories: Array<{ startedAt: Date | number; isAbandoned: boolean }>,
): MilestonesResult
```

### Section UI dans HomeScreen

```tsx
{milestonesData && milestonesData.currentStreak > 0 && (
  <View style={styles.milestonesCard}>
    <View style={styles.milestonesHeader}>
      <Text style={styles.milestonesTitle}>{t.home.milestones.title}</Text>
      <Text style={styles.milestonesStreak}>
        {milestonesData.currentStreak}j {t.home.milestones.streak}
      </Text>
    </View>
    <ScrollView horizontal showsHorizontalScrollIndicator={false} style={styles.milestonesScroll}>
      {milestonesData.milestones.map(m => (
        <View key={m.days} style={[
          styles.milestoneBadge,
          { backgroundColor: m.reached ? colors.primary : colors.background,
            opacity: m.reached ? 1 : 0.4 },
        ]}>
          <Text style={styles.milestoneIcon}>{m.icon}</Text>
          <Text style={[styles.milestoneDays, { color: m.reached ? '#fff' : colors.textSecondary }]}>
            {m.days}j
          </Text>
        </View>
      ))}
    </ScrollView>
    {milestonesData.nextMilestone && (
      <View style={styles.nextMilestoneRow}>
        <View style={styles.nextMilestoneBarTrack}>
          <View style={[styles.nextMilestoneBarFill, {
            width: `${milestonesData.progressToNext}%`,
          }]} />
        </View>
        <Text style={styles.nextMilestoneLabel}>
          {milestonesData.daysToNext}j → {milestonesData.nextMilestone.label}
        </Text>
      </View>
    )}
  </View>
)}
```

**Styles :**
```ts
milestonesCard: {
  marginHorizontal: spacing.md, marginTop: spacing.md,
  backgroundColor: colors.card, borderRadius: borderRadius.lg, padding: spacing.md,
},
milestonesHeader: {
  flexDirection: 'row', justifyContent: 'space-between', marginBottom: spacing.sm,
},
milestonesTitle: { fontSize: fontSize.sm, fontWeight: '700', color: colors.text },
milestonesStreak: { fontSize: fontSize.caption, fontWeight: '600', color: colors.primary },
milestonesScroll: { marginBottom: spacing.sm },
milestoneBadge: {
  width: 48, height: 48, borderRadius: borderRadius.sm,
  alignItems: 'center', justifyContent: 'center', marginRight: spacing.sm,
},
milestoneIcon: { fontSize: fontSize.lg },
milestoneDays: { fontSize: fontSize.caption, fontWeight: '600' },
nextMilestoneRow: { flexDirection: 'row', alignItems: 'center', gap: spacing.sm },
nextMilestoneBarTrack: {
  flex: 1, height: 6, backgroundColor: colors.background,
  borderRadius: borderRadius.xxs, overflow: 'hidden',
},
nextMilestoneBarFill: {
  height: '100%', backgroundColor: colors.primary, borderRadius: borderRadius.xxs,
},
nextMilestoneLabel: { fontSize: fontSize.caption, color: colors.textSecondary },
```

### Traductions

**fr.ts — dans `home: {`** :
```ts
milestones: {
  title: 'Jalons de streak',
  streak: 'de suite',
  nextIn: 'Prochain dans',
  labels: {
    3: '3 jours',
    7: '1 semaine',
    14: '2 semaines',
    30: '1 mois',
    60: '2 mois',
    100: '100 jours',
    200: '200 jours',
    365: '1 an',
  },
},
```

**en.ts :**
```ts
milestones: {
  title: 'Streak milestones',
  streak: 'in a row',
  nextIn: 'Next in',
  labels: {
    3: '3 days',
    7: '1 week',
    14: '2 weeks',
    30: '1 month',
    60: '2 months',
    100: '100 days',
    200: '200 days',
    365: '1 year',
  },
},
```

## Contraintes
- Ephemere — pas de DB
- Ne PAS ajouter `as const` dans en.ts
- Utiliser `textSecondary` (pas textMuted)
- Ne pas modifier withObservables
- Streak = jours d'entrainement consecutifs (1 jour off tolere)

## Criteres de validation
- `npx tsc --noEmit` → zero erreur
- Section milestones visible sur HomeScreen si streak > 0
- Badges atteints en couleur, non-atteints grises
- Barre de progression vers le prochain jalon
- ScrollView horizontal pour les badges

## Git
```bash
git add mobile/src/model/utils/streakMilestonesHelpers.ts \
        mobile/src/screens/HomeScreen.tsx \
        mobile/src/i18n/fr.ts \
        mobile/src/i18n/en.ts
git commit -m "feat(home): streak milestones — visual badges for training consistency achievements"
git push origin develop
```

## Statut
⏳ En attente
