<!-- v1.0 — 2026-03-15 -->
# Rapport — Analyse du Split d'Entraînement (#98) — Groupe C — 20260315-1130

## Objectif
Créer un écran `StatsTrainingSplitScreen` qui analyse automatiquement le pattern d'entraînement de l'utilisateur (Push/Pull/Legs, Upper/Lower, Full Body, etc.) et affiche la répartition des séances par type de split sur les 30 derniers jours. Accessible depuis StatsScreen.

## Fichiers concernés
- `mobile/src/model/utils/trainingSplitHelpers.ts` — NOUVEAU : détection du split
- `mobile/src/screens/StatsTrainingSplitScreen.tsx` — NOUVEAU : écran principal
- `mobile/src/navigation/index.tsx` — MODIFIER : ajouter route `StatsTrainingSplit`
- `mobile/src/screens/StatsScreen.tsx` — MODIFIER : ajouter bouton dans STAT_BUTTONS
- `mobile/src/i18n/fr.ts` — MODIFIER : ajouter section `trainingSplit.*`
- `mobile/src/i18n/en.ts` — MODIFIER : même structure

## Contexte technique

### Stack & Architecture
- React Native + Expo 52 + TypeScript strict (Fabric / New Architecture)
- WatermelonDB — données via `withObservables`
- `useColors()` / `useLanguage()` / `useDeferredMount()`
- `MUSCLES_LIST` depuis `mobile/src/model/constants.ts` :
  `['Pecs', 'Dos', 'Quadriceps', 'Ischios', 'Mollets', 'Trapèzes', 'Epaules', 'Biceps', 'Triceps', 'Abdos', 'Cardio']`

### Modèles disponibles
```ts
// Exercise — table 'exercises'
exercise.muscles: string[]

// Set — table 'sets'
set.exerciseId: string
set.createdAt: Date

// History — table 'histories'
history.createdAt: Date | number
history.deletedAt: Date | null
history.isAbandoned: boolean
```

## Implémentation

### trainingSplitHelpers.ts

```ts
export type SplitType =
  | 'push'         // Pecs, Epaules, Triceps
  | 'pull'         // Dos, Trapèzes, Biceps
  | 'legs'         // Quadriceps, Ischios, Mollets
  | 'upper'        // Push + Pull muscles
  | 'lower'        // Legs muscles
  | 'fullBody'     // ≥ 3 catégories touchées
  | 'cardio'       // Cardio uniquement
  | 'arms'         // Biceps + Triceps dominants
  | 'other'        // Inclassable

export interface SessionSplit {
  date: number
  splitType: SplitType
  muscles: string[]           // muscles travaillés
  exerciseCount: number
  setsCount: number
}

export interface SplitAnalysis {
  sessions: SessionSplit[]
  distribution: Record<SplitType, number>  // nombre de séances par type
  dominantPattern: string                   // clé i18n du pattern détecté
  consistency: number                       // 0-100 : régularité du pattern
}

// Mapping muscles → catégorie
const PUSH_MUSCLES = ['Pecs', 'Epaules', 'Triceps']
const PULL_MUSCLES = ['Dos', 'Trapèzes', 'Biceps']
const LEG_MUSCLES = ['Quadriceps', 'Ischios', 'Mollets']

/**
 * Classifie une séance par type de split basé sur les muscles travaillés.
 */
export function classifySession(muscles: string[]): SplitType

/**
 * Analyse le pattern d'entraînement sur les N derniers jours.
 */
export function analyzeTrainingSplit(
  sets: Array<{ exerciseId: string; createdAt: Date | number }>,
  exercises: Array<{ id: string; muscles: string[] }>,
  histories: Array<{ createdAt: Date | number; deletedAt: Date | null; isAbandoned: boolean }>,
  periodDays?: number,  // défaut: 30
): SplitAnalysis
```

**Algorithme classifySession :**
1. Compter les muscles dans chaque catégorie (push, pull, legs)
2. Si ≥ 3 catégories avec au moins 1 muscle → 'fullBody'
3. Si only cardio → 'cardio'
4. Si push dominant (>50% des muscles) → 'push'
5. Si pull dominant → 'pull'
6. Si legs dominant → 'legs'
7. Si push + pull sans legs → 'upper'
8. Si legs sans push/pull → 'lower'
9. Si biceps + triceps dominants → 'arms'
10. Sinon → 'other'

**Algorithme analyzeTrainingSplit :**
1. Grouper les sets par jour (même jour = même séance)
2. Filtrer les histories valides (non supprimées, non abandonnées)
3. Pour chaque séance : trouver les muscles via exercises, classer via classifySession
4. Calculer la distribution
5. Détecter le `dominantPattern` :
   - Si push+pull+legs chacun ≥ 20% → 'ppl' (Push/Pull/Legs)
   - Si upper+lower chacun ≥ 30% → 'upperLower'
   - Si fullBody ≥ 50% → 'fullBody'
   - Sinon → 'mixed'
6. `consistency` = % de séances qui suivent le pattern dominant

### StatsTrainingSplitScreen.tsx — UI

```
Header: "Analyse du Split"

Pattern détecté :
  ┌────────────────────────────────────────────┐
  │ 🏋️ Push / Pull / Legs                      │
  │ Régularité : 78%                           │
  └────────────────────────────────────────────┘

Distribution (barres horizontales) :
  Push      ████████████  6 séances
  Pull      ██████████    5 séances
  Legs      ████████      4 séances
  Full Body ████          2 séances
  Cardio    ██            1 séance

Timeline (30 derniers jours) :
  [🟢] [🔵] [🟡] [  ] [  ] [🟢] [🔵] ...
  Push  Pull  Legs         Push  Pull
```

**Détails UI :**
- Carte pattern détecté en haut (fond `colors.card`)
- Barres horizontales de distribution (couleur par type de split)
- Timeline en bas : pastilles colorées par jour (ScrollView horizontal)
- Couleurs par type :
  - push: `colors.primary`
  - pull: `'#8B5CF6'` (violet)
  - legs: `'#F59E0B'` (jaune)
  - upper: `colors.primary`
  - lower: `'#F59E0B'`
  - fullBody: `'#10B981'` (vert)
  - cardio: `colors.danger`
  - arms: `'#EC4899'` (rose)
  - other: `colors.textMuted`

### withObservables pattern
```ts
const enhance = withObservables([], () => ({
  sets: database.get<WorkoutSet>('sets').query(
    Q.on('histories', Q.and(
      Q.where('deleted_at', null),
      Q.or(Q.where('is_abandoned', null), Q.where('is_abandoned', false)),
    )),
  ).observe(),
  exercises: database.get<Exercise>('exercises').query().observe(),
  histories: database.get<History>('histories').query(
    Q.where('deleted_at', null),
    Q.or(Q.where('is_abandoned', null), Q.where('is_abandoned', false)),
  ).observe(),
}))
```

### Navigation + StatsScreen
- Lazy import : `const StatsTrainingSplitScreen = React.lazy(() => import('../screens/StatsTrainingSplitScreen'))`
- Route : `StatsTrainingSplit: undefined`
- Stack.Screen : `<Stack.Screen name="StatsTrainingSplit" component={StatsTrainingSplitScreen} options={{ title: t.navigation.statsTrainingSplit }} />`
- Bouton : `{ route: 'StatsTrainingSplit' as const, icon: 'git-branch-outline', label: t.stats.trainingSplit }`

### Traductions

**fr.ts — dans `navigation: {` :**
```ts
statsTrainingSplit: 'Analyse du Split',
```

**fr.ts — dans `stats: {` :**
```ts
trainingSplit: 'Split',
```

**fr.ts — nouvelle section :**
```ts
trainingSplit: {
  title: 'Analyse du Split',
  detectedPattern: 'Pattern détecté',
  consistency: 'Régularité',
  distribution: 'Répartition',
  sessions: 'séances',
  patterns: {
    ppl: 'Push / Pull / Legs',
    upperLower: 'Upper / Lower',
    fullBody: 'Full Body',
    mixed: 'Mixte',
  },
  types: {
    push: 'Push',
    pull: 'Pull',
    legs: 'Legs',
    upper: 'Upper',
    lower: 'Lower',
    fullBody: 'Full Body',
    cardio: 'Cardio',
    arms: 'Bras',
    other: 'Autre',
  },
  noData: 'Pas assez de données — entraînez-vous quelques semaines !',
},
```

**en.ts :**
```ts
trainingSplit: {
  title: 'Split Analysis',
  detectedPattern: 'Detected pattern',
  consistency: 'Consistency',
  distribution: 'Distribution',
  sessions: 'sessions',
  patterns: {
    ppl: 'Push / Pull / Legs',
    upperLower: 'Upper / Lower',
    fullBody: 'Full Body',
    mixed: 'Mixed',
  },
  types: {
    push: 'Push',
    pull: 'Pull',
    legs: 'Legs',
    upper: 'Upper',
    lower: 'Lower',
    fullBody: 'Full Body',
    cardio: 'Cardio',
    arms: 'Arms',
    other: 'Other',
  },
  noData: 'Not enough data — train for a few weeks!',
},
```

## Contraintes
- Pas de migration DB
- `useDeferredMount()` comme wrapper
- Pas de dépendances npm supplémentaires
- Classification déterministe (pas de ML)

## Critères de validation
- `npx tsc --noEmit` → zéro erreur
- Bouton "Split" dans StatsScreen
- Navigation fonctionne
- Pattern PPL/Upper-Lower/FullBody/Mixed correctement détecté
- Barres de distribution proportionnelles

## Dépendances
Aucune dépendance sur les autres groupes.

## Git
```bash
git add mobile/src/model/utils/trainingSplitHelpers.ts \
        mobile/src/screens/StatsTrainingSplitScreen.tsx \
        mobile/src/navigation/index.tsx \
        mobile/src/screens/StatsScreen.tsx \
        mobile/src/i18n/fr.ts \
        mobile/src/i18n/en.ts
git commit -m "feat(stats): training split analysis — auto-detect PPL/Upper-Lower/FullBody pattern"
git push origin develop
```

## Statut
⏳ En attente
