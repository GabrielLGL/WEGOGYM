<!-- v1.0 — 2026-03-14 -->
# Rapport — Cartes d'Exercice (#83) — Groupe D — 20260314-0600

## Objectif
Créer un écran `ExerciseCardScreen` affichant une "fiche trophée" complète d'un exercice : nom, muscles, meilleur 1RM, tonnage total, nombre de séances, date du premier et dernier PR, niveau d'expertise. Accessible depuis `StatsHallOfFameScreen` (tap sur une ligne → navigate to `ExerciseCard`).

## Fichiers concernés
- `mobile/src/screens/ExerciseCardScreen.tsx` — NOUVEAU : écran fiche exercice
- `mobile/src/navigation/index.tsx` — MODIFIER : ajouter route `ExerciseCard`
- `mobile/src/screens/StatsHallOfFameScreen.tsx` — MODIFIER : navigation au tap sur un item
- `mobile/src/i18n/fr.ts` — MODIFIER : ajouter section `exerciseCard.*`
- `mobile/src/i18n/en.ts` — MODIFIER : même structure

## Contexte technique

### Stack & Architecture
- React Native + Expo 52 + TypeScript strict (Fabric / New Architecture)
- WatermelonDB — données via `withObservables`
- `useColors()` / `useLanguage()` / `useDeferredMount()`
- Route param : `{ exerciseId: string }`

### Modèles disponibles
```ts
// Exercise — table 'exercises'
exercise.id: string
exercise.name: string
exercise.muscles: string[]

// Set — table 'sets'
set.weight: number
set.reps: number
set.isPr: boolean
set.createdAt: Date
```

### Formule 1RM Epley
```ts
import { EPLEY_FORMULA_DIVISOR } from '../model/constants'
// 1RM = weight * (1 + reps / EPLEY_FORMULA_DIVISOR)
```

## Implémentation

### Navigation params

```ts
// Dans RootStackParamList :
ExerciseCard: { exerciseId: string };
```

### ExerciseCardScreen.tsx

**withObservables pattern** (observe l'exercice + ses sets) :
```ts
const enhance = withObservables(
  ['exerciseId'],
  ({ exerciseId }: { exerciseId: string }) => ({
    exercise: database.get<Exercise>('exercises').findAndObserve(exerciseId),
    sets: database.get<WorkoutSet>('sets').query(
      Q.on('histories', Q.and(
        Q.where('deleted_at', null),
        Q.or(Q.where('is_abandoned', null), Q.where('is_abandoned', false)),
      )),
      Q.where('exercise_id', exerciseId),
    ).observe(),
  })
)
```

**Calculs dans useMemo :**
```ts
// 1RM estimé (Epley) pour chaque set
const best1RM = useMemo(() => {
  if (!sets.length) return 0
  return Math.max(...sets.map(s => Math.round(s.weight * (1 + s.reps / EPLEY_FORMULA_DIVISOR))))
}, [sets])

const totalTonnage = useMemo(() =>
  sets.reduce((sum, s) => sum + s.weight * s.reps, 0), [sets])

const totalSessions = useMemo(() => {
  // Compter les dates uniques de sets (approximation du nombre de séances)
  const dates = new Set(sets.map(s => s.createdAt?.toDateString()).filter(Boolean))
  return dates.size
}, [sets])

const prSets = useMemo(() => sets.filter(s => s.isPr), [sets])

const firstPRDate = useMemo(() =>
  prSets.length > 0
    ? new Date(Math.min(...prSets.map(s => s.createdAt?.getTime() ?? Infinity)))
    : null, [prSets])

const lastPRDate = useMemo(() =>
  prSets.length > 0
    ? new Date(Math.max(...prSets.map(s => s.createdAt?.getTime() ?? 0)))
    : null, [prSets])

// Niveau d'expertise basé sur le nombre de séances
const expertiseLevel = useMemo((): 'debutant' | 'intermediaire' | 'avance' | 'expert' => {
  if (totalSessions < 5) return 'debutant'
  if (totalSessions < 15) return 'intermediaire'
  if (totalSessions < 30) return 'avance'
  return 'expert'
}, [totalSessions])
```

### UI — ExerciseCardScreen

```
ScrollView :
  ┌──────────────────────────────────────┐
  │         🏆 SQUAT BARRE               │
  │       Quadriceps · Ischios           │
  │                                      │
  │   1RM estimé    Tonnage total        │
  │    ~137 kg      47 850 kg            │
  │                                      │
  │   Séances       PRs                  │
  │     34          12                   │
  │                                      │
  │   Expertise : [●●●○] Avancé          │
  └──────────────────────────────────────┘

  Section historique PRs :
    Premier PR : 12 mars 2025
    Dernier PR  : 8 fév 2026
```

**Détails UI :**
- Grande card principale : fond `colors.card`, centré
- Nom exercice en `fontSize.xl`, `fontWeight: '900'`, `textTransform: 'uppercase'`
- Muscles : chips colorés (style muscleChip)
- Grille 2×2 de KPIs : 1RM / Tonnage / Séances / PRs
- Niveau expertise : 4 dots (●/○) + label
- Dates en `colors.textSecondary`

### Intégration dans StatsHallOfFameScreen

Lire `StatsHallOfFameScreen.tsx`. Trouver le composant qui rend chaque ligne de la FlatList (l'item avec le rang + nom exercice + 1RM). Lui ajouter un `onPress` :

```tsx
// Sur le TouchableOpacity ou View de chaque item :
onPress={() => navigation.navigate('ExerciseCard', { exerciseId: entry.exerciseId })}
```

**Note :** `StatsHallOfFameScreen` reçoit probablement `navigation` via props. Vérifier qu'il est bien disponible. Si c'est un composant enhanced sans navigation prop, utiliser `useNavigation()` depuis `@react-navigation/native`.

### Navigation — modifier navigation/index.tsx
**Lazy import** (après `ExerciseCollectionScreen`) :
```ts
const ExerciseCardScreen = React.lazy(() => import('../screens/ExerciseCardScreen'))
```

**RootStackParamList** (après `ExerciseCollection: undefined`) :
```ts
ExerciseCard: { exerciseId: string };
```

**Stack.Screen** :
```tsx
<Stack.Screen name="ExerciseCard" component={ExerciseCardScreen} options={{ title: '' }} />
```

### Traductions

**fr.ts — dans `navigation: {`** : pas besoin (title vide, nom affiché dynamiquement)

**fr.ts — nouvelle section (avant `share: {`) :**
```ts
exerciseCard: {
  orm: '1RM estimé',
  tonnage: 'Tonnage total',
  sessions: 'Séances',
  prs: 'PRs',
  expertise: 'Expertise',
  expertiseLevels: {
    debutant: 'Débutant',
    intermediaire: 'Intermédiaire',
    avance: 'Avancé',
    expert: 'Expert',
  },
  firstPR: 'Premier PR',
  lastPR: 'Dernier PR',
  noPRYet: 'Aucun PR encore',
},
```

**en.ts :**
```ts
exerciseCard: {
  orm: 'Est. 1RM',
  tonnage: 'Total tonnage',
  sessions: 'Sessions',
  prs: 'PRs',
  expertise: 'Expertise',
  expertiseLevels: {
    debutant: 'Beginner',
    intermediaire: 'Intermediate',
    avance: 'Advanced',
    expert: 'Expert',
  },
  firstPR: 'First PR',
  lastPR: 'Last PR',
  noPRYet: 'No PR yet',
},
```

## Contraintes
- `withObservables` avec param `exerciseId` — pattern `findAndObserve` pour l'exercice
- `useDeferredMount()` comme wrapper
- Si `exercise` null (exercice supprimé) → afficher empty state gracieux
- La modification de `StatsHallOfFameScreen` : **uniquement ajouter `onPress`**, pas de refactor

## Critères de validation
- `npx tsc --noEmit` → zéro erreur
- Tap sur un exercice dans StatsHallOfFameScreen → ouvre ExerciseCardScreen
- KPIs corrects (1RM, tonnage, séances, PRs)
- Niveau d'expertise s'affiche
- Aucun crash si exercice sans PRs

## Dépendances
Aucune dépendance sur les autres groupes.

## Git
```bash
git add mobile/src/screens/ExerciseCardScreen.tsx \
        mobile/src/navigation/index.tsx \
        mobile/src/screens/StatsHallOfFameScreen.tsx \
        mobile/src/i18n/fr.ts \
        mobile/src/i18n/en.ts \
        docs/bmad/do/[rapport]
git commit -m "feat(stats): exercise card screen — trophy profile with 1RM/tonnage/expertise + tap from Hall of Fame"
git push origin develop
# Si remote ahead : git pull --rebase puis re-push
```

## Statut
✅ Résolu — 20260314-0620

## Résolution
Rapport do : docs/bmad/do/20260314-0620-feat-exercise-card.md
