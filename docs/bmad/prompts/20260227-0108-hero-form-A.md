<!-- v1.0 — 2026-02-27 -->
# Rapport — hero-form — Groupe A — 20260227-0108

## Objectif
Dupliquer le formulaire d'inscription email/prénom dans la section Hero de `web/src/app/page.tsx`, juste après le bouton "Rejoindre la beta". Le formulaire dupliqué DOIT partager le même state (`email`, `name`, `status`) et la même fonction `handleSubmit` que le formulaire existant dans la section `#download` (en bas de page). Style neumorphique cohérent.

## Fichiers concernés
- `web/src/app/page.tsx`

## Contexte technique

### Structure actuelle du Hero (lignes 171–216)
```
<header id="main-content" ...>
  Logo pill (hero-fade)
  <h1> titre (hero-fade)
  <p> sous-titre (hero-fade)
  Stats row (hero-fade grid grid-cols-3)
  <a href="#download" ...>Rejoindre la beta</a>   ← CTA bouton (ligne 210-215)
</header>
```

### Formulaire existant (section #download, lignes 314–368)
- State partagé : `email`, `name`, `status` (déjà définis dans le composant)
- Handler : `handleSubmit` (déjà défini)
- IDs HTML : `subscribe-name` et `subscribe-email` (NE PAS dupliquer ces IDs — HTML invalide)
- Style inputs : `bg-[var(--bg)] rounded-full shadow-neu-out p-2.5` (wrapper), `shadow-neu-in` (input)
- Bouton submit : `btn-liquid text-white py-4 rounded-full font-extrabold text-base uppercase tracking-widest`

### Neumorphisme
- Wrapper inputs : `bg-[var(--bg)] rounded-full shadow-neu-out p-2.5`
- Input lui-même : `shadow-neu-in` avec `bg-transparent border-none outline-none`
- Bouton : classe `btn-liquid text-white`

## Étapes

1. **Ouvrir** `web/src/app/page.tsx`

2. **Localiser** la ligne du bouton CTA Hero (autour de la ligne 210) :
   ```tsx
   <a
     href="#download"
     className="hero-fade btn-liquid text-white px-10 py-4 rounded-full font-extrabold text-lg uppercase tracking-widest no-underline"
   >
     Rejoindre la beta
   </a>
   ```

3. **Insérer** juste APRÈS ce `<a>` (avant la fermeture `</header>`), le formulaire suivant :

```tsx
{/* Hero inline form */}
<form
  onSubmit={handleSubmit}
  className="hero-fade mt-8 space-y-3 w-full max-w-sm mx-auto"
  aria-label="Formulaire d'inscription rapide"
>
  <div className="bg-[var(--bg)] rounded-full shadow-neu-out p-2.5">
    <label htmlFor="hero-name" className="sr-only">Prénom (optionnel)</label>
    <input
      id="hero-name"
      type="text"
      placeholder="Ton prénom (optionnel)"
      value={name}
      onChange={(e) => setName(e.target.value)}
      className="w-full bg-transparent border-none py-2.5 px-5 rounded-full shadow-neu-in
        text-[var(--text-main)] font-inherit text-sm outline-none
        placeholder:text-[var(--text-muted)] placeholder:opacity-60"
    />
  </div>

  <div className="bg-[var(--bg)] rounded-full shadow-neu-out p-2.5">
    <label htmlFor="hero-email" className="sr-only">Adresse email</label>
    <input
      id="hero-email"
      type="email"
      placeholder="Ton email"
      value={email}
      onChange={(e) => setEmail(e.target.value)}
      required
      aria-required="true"
      className="w-full bg-transparent border-none py-2.5 px-5 rounded-full shadow-neu-in
        text-[var(--text-main)] font-inherit text-sm outline-none
        placeholder:text-[var(--text-muted)] placeholder:opacity-60"
    />
  </div>

  <button
    type="submit"
    disabled={status === "loading"}
    className="w-full btn-liquid text-white py-3.5 rounded-full font-extrabold text-sm
      uppercase tracking-widest border-none cursor-pointer
      disabled:opacity-50 disabled:cursor-not-allowed"
  >
    {status === "loading" ? "Inscription..." : "S\u2019inscrire à la beta"}
  </button>

  {status === "success" && (
    <p role="alert" className="text-[var(--success)] text-xs font-semibold text-center">
      Inscription réussie ! Vérifie ta boite mail.
    </p>
  )}
  {status === "error" && (
    <p role="alert" className="text-[var(--danger)] text-xs font-semibold text-center">
      Une erreur est survenue. Réessaie.
    </p>
  )}
</form>
```

4. **Vérifier** que les IDs (`hero-name`, `hero-email`) sont différents des IDs du formulaire bas de page (`subscribe-name`, `subscribe-email`) — pas de duplication d'ID HTML.

5. **Vérifier** que le state et le handler ne sont PAS dupliqués — utiliser uniquement les existants (`email`, `name`, `status`, `handleSubmit`).

## Contraintes
- Ne PAS modifier le formulaire existant dans `#download` (lignes 314–368)
- Ne PAS créer de nouveau state ou nouveau handler
- Ne PAS dupliquer les IDs HTML (`subscribe-name`, `subscribe-email`) → utiliser `hero-name`, `hero-email`
- Respecter le style neumorphique : `shadow-neu-out` (wrapper), `shadow-neu-in` (input), `btn-liquid` (bouton)
- Ajouter la classe `hero-fade` sur `<form>` pour l'animation d'apparition hero
- Texte du bouton : "S'inscrire à la beta" (différencier visuellement du CTA "Rejoindre la beta")

## Critères de validation
- Le formulaire Hero apparaît visuellement juste sous le bouton "Rejoindre la beta"
- Saisir un email dans Hero → même state que le formulaire en bas (les deux champs se synchronisent)
- Soumettre depuis Hero → déclenche `handleSubmit` → message de succès/erreur affiché dans les deux formulaires
- `cd web && npx tsc --noEmit` → zéro erreur TypeScript
- Pas d'IDs HTML dupliqués (vérifier avec Ctrl+F `hero-name`, `subscribe-name`)

## Dépendances
Aucune dépendance — groupe unique.

## Statut
✅ Résolu — 20260227-0111

## Résolution
Rapport do : docs/bmad/do/20260227-0111-feat-hero-form.md
