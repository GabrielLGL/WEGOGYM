# Cohérence WatermelonDB — 20260307-0304

## Résultat : ✅ Cohérent

### Schéma ↔ Modèles

10 tables dans le schéma v32, 10 modèles correspondants. Aucune incohérence.

| Table | Colonnes schéma | Décorateurs modèle | Statut |
|-------|----------------|---------------------|--------|
| `programs` | 6 | 6 + @children(sessions) | ✅ |
| `sessions` | 6 | 6 + @relation(program) + @children(histories, session_exercises) | ✅ |
| `session_exercises` | 14 | 14 + @relation(session, exercise) | ✅ |
| `exercises` | 9 | 9 + @children(session_exercises, performance_logs, sets) | ✅ |
| `performance_logs` | 5 | 5 + @relation(exercise) | ✅ |
| `users` | 27 | 27 | ✅ |
| `user_badges` | 4 | 4 | ✅ |
| `body_measurements` | 8 | 8 | ✅ |
| `histories` | 7 | 7 + @relation(session) + @children(sets) | ✅ |
| `sets` | 8 | 8 + @relation(history, exercise) | ✅ |

### Incohérences schéma ↔ modèles

| Table | Problème |
|-------|----------|
| *(aucune)* | — |

### Relations

Toutes les relations sont cohérentes :

| Modèle | Relation | Type | Cible | Statut |
|--------|----------|------|-------|--------|
| Program | sessions | has_many | sessions.program_id | ✅ |
| Session | program | belongs_to | programs | ✅ |
| Session | histories | has_many | histories.session_id | ✅ |
| Session | sessionExercises | has_many | session_exercises.session_id | ✅ |
| SessionExercise | session | belongs_to | sessions | ✅ |
| SessionExercise | exercise | belongs_to | exercises | ✅ |
| Exercise | session_exercises | has_many | session_exercises.exercise_id | ✅ |
| Exercise | performance_logs | has_many | performance_logs.exercise_id | ✅ |
| Exercise | sets | has_many | sets.exercise_id | ✅ |
| PerformanceLog | exercise | belongs_to | exercises | ✅ |
| History | session | belongs_to | sessions | ✅ |
| History | sets | has_many | sets.history_id | ✅ |
| Set | history | belongs_to | histories | ✅ |
| Set | exercise | belongs_to | exercises | ✅ |

### Migrations

✅ Fichier `migrations.ts` présent — couvre v27 → v32, cohérent avec le schéma v32.

| Version | Changement |
|---------|------------|
| v27 | +superset_id, superset_type, superset_position → session_exercises |
| v28 | +tutorial_completed → users |
| v29 | +notes → session_exercises |
| v30 | +rest_time → session_exercises |
| v31 | +reminders_enabled, reminder_days, reminder_hour, reminder_minute → users |
| v32 | Suppression ai_api_key du schéma (colonne physique conservée, données migrées vers expo-secure-store) |

### Types

Tous les types de colonnes (string/number/boolean) correspondent aux décorateurs utilisés (@text/@field/@date/@readonly @date).

Note : `body_measurements.date` utilise `@field` (number) au lieu de `@date` — choix intentionnel (timestamp brut), cohérent schéma ↔ modèle.

Note : `performance_logs` n'a pas de `updated_at` — table append-only, cohérent schéma ↔ modèle.
