# Tests — 20260228-1500

## Résultat : ✅ 1563 passed / 🔴 0 failed / ⏭️ 0 skipped

**Suites :** 94 / 94 passées — 0 échec — durée ~76 s

---

### Tests en échec

Aucun.

---

### Couverture

Non collectée (run sans `--coverage`).

---

### Suites passantes (94)

**Screens (20)**
- ExercisesScreen, SessionDetailScreen, SettingsScreen, AssistantScreen, WorkoutScreen
- StatsRepartitionScreen, OnboardingScreen, StatsCalendarScreen, StatsMeasurementsScreen
- ChartsScreen, ProgramsScreen, HomeScreen, StatsDurationScreen, StatsVolumeScreen
- StatsExercisesScreen, StatsScreen, BadgesScreen, ExerciseHistoryScreen
- AssistantPreviewScreen, ProgramDetailScreen

**Components (21)**
- RestTimer, ExercisePickerModal, WorkoutSummarySheet, ProgramDetailBottomSheet
- WorkoutExerciseCard, AlertDialog, SessionItem, AssistantPreviewSheet, ChipSelector
- ExerciseInfoSheet, SessionExerciseItem, CustomModal, ExerciseTargetInputs
- OnboardingSheet, BottomSheet, ProgramSection, Button, StreakIndicator
- MilestoneCelebration, LastPerformanceBadge, SetItem, WorkoutHeader
- BadgeCelebration, OnboardingCard, XPProgressBar, ErrorBoundary, HeatmapCalendar
- BadgeCard, LevelBadge

**Hooks (9)**
- useSessionManager, useProgramManager, useExerciseManager, useWorkoutState
- useWorkoutTimer, useKeyboardAnimation, useExerciseFilters, useHaptics, useModalState

**Contexts (1)** — ThemeContext

**Model / Utils (22)**
- seed, constants, models, databaseHelpers, statsHelpers, workoutSessionUtils
- workoutSetUtils, exerciseStatsUtils, programImportUtils, statsVolume, statsMuscle
- parseUtils, statsKPIs, gamificationHelpers, statsDuration, progressionHelpers
- validationHelpers, statsPRs, exportHelpers, exerciseQueryUtils, badgeHelpers, statsDateUtils

**Services (11)**
- ai/providers, ai/aiService, ai/offlineEngine, ai/providerUtils
- notificationService, secureKeyStore, sentry
- programGenerator: volumeCalculator, splitStrategy, exerciseSelector, tables, sessionBuilder

**Theme (1)** — chartConfig

---

### Warnings non-bloquants

- `console.error` : React state updates hors `act()` — `ExercisesScreen.tsx` lignes 102-106
  (`navigation.addListener('focus', ...)` — comportement async normal en test, non-bloquant)

---

### Fichiers critiques sans tests

Aucun fichier critique identifié comme manquant de couverture.
Les helpers récemment modifiés (`gamificationHelpers.ts`, `badgeConstants.ts`) ont leurs suites (`gamificationHelpers.test.ts`, `badgeHelpers.test.ts`).
