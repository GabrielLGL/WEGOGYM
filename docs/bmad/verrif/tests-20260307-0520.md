# Tests — 20260307-0520

## Résultat : ✅ 1685 passed / 🔴 2 failed / ⏭️ 0 skipped

106 suites (105 passed, 1 failed) — temps d'exécution : 44s

### Tests en échec
| Test | Fichier | Erreur |
|------|---------|--------|
| affiche les séances du jour sélectionné | StatsCalendarScreen.test.tsx | `queryByText('PPL Push')` → null (date-dependent flaky test) |
| affiche la durée dans la carte pour une séance avec endTime | StatsCalendarScreen.test.tsx | `queryByText('1h')` → null (date-dependent flaky test) |

**Cause** : Tests date-dependent pré-existants (commit `ed2b22a`). Non introduits par les modifications actuelles. Les tests dépendent de `todayKey` dynamique et du rendu du calendrier qui exclut les jours hors-mois.

### Fichiers critiques sans tests
Tous les hooks et helpers critiques sont couverts à 80%+. Restent sans tests :
- `model/migrations.ts` (infrastructure, pas de logique testable)
- `model/seedDevData.ts` (dev only)
- `types/workout.ts` et `services/ai/types.ts` (types uniquement)

### Couverture
- Statements : 77.00%
- Branches : 67.80%
- Functions : 72.00%
- Lines : 79.01%
