# Code Review — 20260307-0315

## Résultat : 10 problèmes trouvés

### 🔴 Critiques
| # | Fichier | Ligne | Problème | Impact |
|---|---------|-------|----------|--------|
| 1 | `components/OnboardingSheet.tsx` | 5 | Import statique `import { colors } from '../theme'` au lieu de `useColors()`. Le composant **ne réagit pas** au changement de thème (dark/light toggle). | UI cassée après switch de thème |
| 2 | `components/WorkoutExerciseCard.tsx` | 227-240 | `database.write()` directement dans le composant (`handleSaveSessionNote`). La logique métier DB doit vivre dans un hook (`useWorkoutState` ou dédié). | Violation architecture, couplage fort |
| 3 | `screens/StatsCalendarScreen.tsx` | 279-389 | `handleDayPress` = **110 lignes** avec pattern N+1 : filtre JS sur toutes les `histories`, puis `.fetch()` individuel par session/program/sets/exercise. | Performance dégradée sur gros datasets |

### 🟡 Warnings
| # | Fichier | Ligne | Problème | Impact |
|---|---------|-------|----------|--------|
| 4 | `hooks/useModalState.ts`, `useExerciseManager.ts`, `useProgramManager.ts`, `useSessionManager.ts` | Toutes fonctions retournées | **~30 fonctions** retournées sans `useCallback`. Chaque re-render parent crée de nouvelles références → re-renders en cascade des enfants. | Performance globale |
| 5 | `model/models/Program.ts:18`, `Session.ts:19-20`, `History.ts:18` | 18-20 | `@children()` sans `@lazy` sur 4 relations (sessions, histories, sessionExercises, sets). WatermelonDB charge ces queries dès l'observation du modèle. | Mémoire + performance |
| 6 | `screens/SettingsScreen.tsx` | Entier | **1470 lignes**. Plus gros fichier du projet. Fonctions longues (`handleDeleteAllData` 70L). Inline `onPress` async avec DB write (L812-824). | Maintenabilité critique |
| 7 | `screens/WorkoutScreen.tsx` | 216-352 | `handleConfirmEnd` = **136 lignes** : complete history + XP + level + tonnage + streak + badges + recap. Devrait être extrait dans un hook `useWorkoutCompletion`. | Lisibilité, testabilité |
| 8 | 8 screens (`HomeScreen`, `WorkoutScreen`, `ExercisesScreen`, `ChartsScreen`, etc.) | `useStyles()` | `StyleSheet.create()` appelé dans `useStyles(colors)` **sans `useMemo`** — recréé à chaque render. | Re-renders inutiles partout |

### 🔵 Suggestions
| # | Fichier | Ligne | Problème | Suggestion |
|---|---------|-------|----------|------------|
| 9 | `components/WorkoutSummarySheet.tsx` | 156,160,165,212,219,237 | 6 inline `style={{ flexDirection:'row', alignItems:'center', gap: spacing.xs }}` identiques — violation DRY + objets recréés. | Extraire dans `styles.row` via `StyleSheet.create` |
| 10 | `components/CoachMarks.tsx:126`, `BottomSheet.tsx:67`, `AlertDialog.tsx:64` | 64-135 | `Animated.timing().start()` sans stocker la ref d'animation. Si unmount pendant l'animation, le callback `.start()` peut fire sur un composant démonté. | Stocker `animRef = Animated.timing(...)` et `animRef.stop()` dans le cleanup |

## Parallélisation des corrections
| # | Problème | Fichiers | Effort | Groupe |
|---|----------|----------|--------|--------|
| 1 | OnboardingSheet thème statique | `OnboardingSheet.tsx` | S | A |
| 2 | DB write dans composant | `WorkoutExerciseCard.tsx`, `hooks/useWorkoutState.ts` | M | B |
| 3 | N+1 queries StatsCalendar | `StatsCalendarScreen.tsx` | L | C |
| 4 | useCallback hooks | `useModalState.ts`, `useExerciseManager.ts`, `useProgramManager.ts`, `useSessionManager.ts` | M | D |
| 5 | @lazy @children | `Program.ts`, `Session.ts`, `History.ts` | S | A |
| 6 | Split SettingsScreen | `SettingsScreen.tsx` | L | C |
| 7 | Extract handleConfirmEnd | `WorkoutScreen.tsx` | M | B |
| 8 | useMemo useStyles | 8 screens | M | D |
| 9 | DRY inline styles | `WorkoutSummarySheet.tsx` | S | A |
| 10 | Animation cleanup | `CoachMarks.tsx`, `BottomSheet.tsx`, `AlertDialog.tsx` | S | A |

Groupes : même lettre = mêmes fichiers (séquentiel). Lettres différentes = parallèle.
- Claude Code 1 : Groupe A (1, 5, 9, 10)
- Claude Code 2 : Groupe B (2, 7)
- Claude Code 3 : Groupe C (3, 6)
- Claude Code 4 : Groupe D (4, 8)
