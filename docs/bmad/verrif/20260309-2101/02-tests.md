# Passe 2/8 — Tests

**Commande :** `npx jest --verbose`
**Résultat :** ✅ 1737 tests passed, 112 suites, 0 fail

**Temps :** 42.5s

**Warnings (non-bloquants) :**
- `act(...)` warnings dans ExercisesScreen tests (state updates outside act) — cosmétique, pas de fail
