# Fix app.json — userInterfaceStyle + Sentry DSN

## Statut : ✅ Résolu — 20260307-1600

## Contexte
Deux corrections dans `app.json` / `app.config.js` :
1. `userInterfaceStyle` est actuellement `"dark"` — doit etre `"automatic"` pour supporter le theme toggle (dark/light)
2. Ajouter le Sentry DSN pour le crash reporting en production

## Actions
1. Lire `app.json` ou `app.config.js`
2. Changer `"userInterfaceStyle": "dark"` → `"userInterfaceStyle": "automatic"`
3. Ajouter la config Sentry si pas deja presente :
   ```json
   "plugins": [
     ["@sentry/react-native/expo", {
       "organization": "...",
       "project": "kore"
     }]
   ]
   ```
4. Verifier que le DSN est dans les variables d'environnement (pas en dur dans le code)
5. Commit : `fix(config): userInterfaceStyle automatic + Sentry DSN`

## Risques
- Faible : changement de config seulement
- Sentry DSN : s'assurer qu'il est dans les env vars EAS, pas dans le code source

## Priorite : BLOQUANT
`userInterfaceStyle: "dark"` empeche le theme clair de fonctionner correctement sur certains devices.

## Résolution
Rapport do : docs/bmad/do/20260307-1600-fix-app-json.md
