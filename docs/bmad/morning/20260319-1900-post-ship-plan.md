# Rapport — Post-ship polish plan — 2026-03-19

## Probleme
Groupes A et C du verrif 20260319-1009 sont resolus. Restent B, D, E pour remonter le score sante a 100/100.

## Fichiers concernes
- Groupe B : HomeScreen.tsx (2082 lignes, monolithique, non-null assertion, fetch imperatif)
- Groupe D : muscleRecoveryHelpers.ts, streakMilestonesHelpers.ts
- Groupe E : overtrainingHelpers.ts (dead code), 12 fichiers tests (as any)

## Commandes a lancer
```
/do Groupe B — HomeScreen refactor : split composants, withObservables, null safety (HomeScreen.tsx)
/do Groupe D — muscleRecovery sort-independent + streak semantique (muscleRecoveryHelpers.ts, streakMilestonesHelpers.ts)
/do Groupe E — supprimer overtrainingHelpers dead code + migrer 12 tests as any vers testFactories
```

## Contexte
- Verrif 20260319-1009 RAPPORT.md contient le detail des 13 issues
- Groupe A (71 couleurs hardcodees) : RESOLU via commit 5314f13
- Groupe C (DRY getMondayOfWeek + useCallback + edge cases) : RESOLU via commit a68cea5
- Ship score 100/100 — aucun bloquant, c'est du polish
- AAB build deja fait, beta testeurs actifs

## Criteres de validation
- HomeScreen < 500 lignes par composant
- 0 code mort
- 0 `as any` dans les tests (utiliser testFactories)
- Score HEALTH.md retour a 100/100
- `npx tsc --noEmit` + `npm test` passent

## Statut
✅ Obsolete — resolu par audit critique 20260320 + verrif runs 20260321. HomeScreen 291 lignes, 0 as any prod, overtrainingHelpers identifie comme code mort restant.
