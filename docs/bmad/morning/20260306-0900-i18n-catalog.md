# 3 strings hardcodees FR dans ExerciseCatalogScreen

## Statut : ✅ Résolu — 20260307 (déjà fait lors des commits i18n antérieurs)

## Contexte
ExerciseCatalogScreen contient 3 strings en francais directement dans le code au lieu d'utiliser le systeme i18n (`useLanguage()` + `t()`).

## Actions
1. Lire `mobile/src/screens/ExerciseCatalogScreen.tsx`
2. Identifier les 3 strings hardcodees
3. Ajouter les cles correspondantes dans `mobile/src/i18n/fr.ts` et `mobile/src/i18n/en.ts`
4. Remplacer les strings hardcodees par `t('cle')`
5. Verifier que le screen fonctionne en FR et EN
6. Commit : `fix(i18n): extract hardcoded FR strings in ExerciseCatalogScreen`

## Risques
- Faible : changement localise
- S'assurer que les cles i18n suivent la convention existante

## Priorite : IMPORTANT
L'app est bilingue FR/EN — toutes les strings doivent passer par i18n.
