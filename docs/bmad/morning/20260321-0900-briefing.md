# Rapport — Dead code cleanup (overtrainingHelpers) — 2026-03-21

## Probleme
`overtrainingHelpers.ts` est du code mort : exporte `computeOvertrainingAlert`, `OvertrainingData`, `OvertrainingSignal` mais aucun screen/composant ne les importe. Seulement re-exporte depuis `statsHelpers.ts` et teste dans son propre fichier test. L'ecran qui l'utilisait a ete supprime lors de l'audit critique 20260320.

## Fichiers concernes
- `mobile/src/model/utils/overtrainingHelpers.ts` (95 lignes) — SUPPRIMER
- `mobile/src/model/utils/__tests__/overtrainingHelpers.test.ts` — SUPPRIMER
- `mobile/src/model/utils/statsHelpers.ts` — retirer `export * from './overtrainingHelpers'`
- `mobile/src/i18n/fr.ts` + `en.ts` — retirer section `overtraining` (si non referencee ailleurs)

## Commande a lancer
/do docs/bmad/morning/20260321-0900-briefing.md

## Contexte
Identifie lors du verrif 20260321-0859 (suggestion) et confirme dans le briefing morning. L'audit critique 20260320 a supprime l'ecran qui utilisait ce helper mais le helper lui-meme est reste orphelin.

## Criteres de validation
- `overtrainingHelpers.ts` et son test supprimes
- Re-export retire de statsHelpers.ts
- `npx tsc --noEmit` → 0 erreur
- `npm test` → 0 fail

## Statut
⏳ En attente
