# Rapport — Stats perf + i18n mois — 2026-03-11

## Problème
1. **`buildWeeklyActivity`** dans `statsVolume.ts` a une complexité O(H*S) — pour chaque history, il itère tous les sets. Solution : pré-indexer les sets par `history_id` dans un Map avant la boucle.
2. **`computeMonthlySetsChart`** dans `statsMuscle.ts` utilise des noms de mois anglais en fallback. Doit utiliser le système i18n existant.

## Fichiers concernés
- `mobile/src/model/utils/statsVolume.ts` — `buildWeeklyActivity`
- `mobile/src/model/utils/statsMuscle.ts` — `computeMonthlySetsChart`
- Tests associés (~15 tests à adapter pour i18n mois)

## Commande à lancer
/do docs/bmad/morning/20260311-1900-stats-perf-i18n.md

## Contexte
### Perf statsVolume
`buildWeeklyActivity` parcourt `histories` et pour chaque history fait un `.filter()` sur `sets`. Créer un `Map<string, Set[]>` groupé par `history_id` avant la boucle, puis lookup O(1).

### i18n statsMuscle
`computeMonthlySetsChart` génère des labels de mois en anglais. Utiliser les clés i18n `t.months.short` (ou équivalent) ou passer les labels de mois en paramètre depuis l'écran appelant.

## Critères de validation
- `npx tsc --noEmit` — 0 erreurs
- `npm test` — tous tests passent (adapter les tests i18n)
- `buildWeeklyActivity` n'a plus de `.filter()` imbriqué dans la boucle histories

## Statut
✅ Résolu — 20260311-1920

## Résolution
Rapport do : docs/bmad/do/20260311-1920-perf-stats-i18n.md
