# Passe 1/3 — Bloquants (TypeScript + Tests)

## TypeScript
- `npx tsc --noEmit` : **0 erreur**

## Tests
- `npx jest --verbose` : **2230 tests passed, 188 suites, 0 fail**
- Temps : ~100s

## Bloquants runtime
**Aucun bloquant detecte.**
