# fix(ui) — Improve Gemini 429 hint — guide user to AI Studio key
Date : 2026-02-19 15:30

## Instruction
Dans mobile/src/screens/SettingsScreen.tsx, mettre à jour le hint 429 dans la fonction handleTestConnection pour guider l'utilisateur vers aistudio.google.com plutôt qu'un message générique sur le quota.

## Classification
Type : fix
Fichiers : mobile/src/screens/SettingsScreen.tsx

## Ce qui a été fait
Remplacement du texte du hint `isGemini429` (ligne 108) :
- Avant : `'\n\nQuota gratuit dépassé. Attendez quelques secondes et réessayez, ou activez la facturation sur console.cloud.google.com.'`
- Après : `'\n\nClé API sans quota gratuit.\n\n• Créez une clé sur aistudio.google.com (gratuit)\n• OU activez la facturation sur console.cloud.google.com'`

Aucune autre modification effectuée dans le fichier.

## Vérification
- TypeScript : ✅ zéro erreur (`npx tsc --noEmit`)
- Tests : non lancés (changement de string UI, pas de logique métier)
- Nouveau test créé : non

## Commit
fix(ui): improve Gemini 429 hint — guide user to AI Studio key
