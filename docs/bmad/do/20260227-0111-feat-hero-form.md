# FEAT(hero-form) — Formulaire d'inscription inline dans la section Hero
Date : 2026-02-27 01:11

## Instruction
docs/bmad/prompts/20260227-0108-hero-form-A.md

## Rapport source
docs/bmad/prompts/20260227-0108-hero-form-A.md

## Classification
Type : feat
Fichiers modifiés : `web/src/app/page.tsx`

## Ce qui a été fait
- Ajout d'un `<form>` inline dans la section Hero, juste après le bouton "Rejoindre la beta"
- Le formulaire utilise les states existants `email`, `name`, `status` et la fonction `handleSubmit` déjà définis — aucun state dupliqué
- IDs HTML uniques : `hero-name` et `hero-email` (vs `subscribe-name`/`subscribe-email` du formulaire en bas)
- Style neumorphique cohérent : `shadow-neu-out` (wrapper), `shadow-neu-in` (inputs), `btn-liquid` (bouton)
- Classe `hero-fade` sur le `<form>` pour l'animation d'apparition
- Texte bouton : "S'inscrire à la beta" (différent du CTA "Rejoindre la beta")
- Messages de succès/erreur affichés dans les deux formulaires simultanément (state partagé)

## Vérification
- TypeScript : ✅ `npx tsc --noEmit` → zéro erreur
- Tests : ✅ (pas de tests web pour ce composant)
- Nouveau test créé : non (UI-only, pas de logique nouvelle)

## Documentation mise à jour
Aucune (pas de nouveau pattern ni composant)

## Statut
✅ Résolu — 20260227-0111

## Commit
[sera rempli après commit]
