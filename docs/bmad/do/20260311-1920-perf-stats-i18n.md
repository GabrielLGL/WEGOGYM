# perf+fix(stats) — Optimisation buildWeeklyActivity + i18n mois
Date : 2026-03-11 19:20

## Instruction
docs/bmad/morning/20260311-1900-stats-perf-i18n.md

## Rapport source
docs/bmad/morning/20260311-1900-stats-perf-i18n.md

## Classification
Type : perf + fix
Fichiers modifiés :
- `mobile/src/model/utils/statsVolume.ts`
- `mobile/src/model/utils/statsMuscle.ts`

## Ce qui a été fait

### 1. Perf `buildWeeklyActivity` (statsVolume.ts)
- Créé un `Map<string, WorkoutSet[]>` pré-indexé par `history.id` avant la boucle principale
- Remplacé `sets.filter(s => s.history.id === h.id)` (O(S) par history) par `setsMap.get(h.id) ?? []` (O(1))
- Complexité réduite de O(H×S) à O(H+S)

### 2. i18n `computeMonthlySetsChart` (statsMuscle.ts)
- Supprimé la constante `MONTH_LABELS_DEFAULT` hardcodée en anglais
- Changé `monthLabels` de paramètre optionnel (`?`) à paramètre avec valeur par défaut FR
- Default : `['Jan', 'Fév', 'Mar', 'Avr', 'Mai', 'Juin', 'Juil', 'Aoû', 'Sep', 'Oct', 'Nov', 'Déc']`
- L'appelant (`StatsVolumeScreen.tsx`) passe déjà `t.statsVolume.monthAbbr` donc aucun changement nécessaire côté écran

## Vérification
- TypeScript : ✅ 0 erreurs
- Tests : ✅ 1694 passed (109 suites)
- Nouveau test créé : non (comportement identique, pas de nouvelle logique)

## Documentation mise à jour
aucune

## Statut
✅ Résolu — 20260311-1920

## Commit
<!-- sera rempli après commit -->
