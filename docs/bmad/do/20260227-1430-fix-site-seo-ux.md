# Fix Site Web — SEO + UX + Accents — 20260227-1430

## Statut : ✅ Terminé — `npx tsc --noEmit` → 0 erreur

---

## Groupe A — SEO Critique

### layout.tsx
- ✅ Description : "seance apres seance" → "séance après séance" (3 occurrences : description, OG, Twitter, JSON-LD)
- ✅ Keywords enrichis : +4 termes ("workout tracker", "carnet entraînement", "appli musculation android", "coach muscu")
- ✅ JSON-LD déplacé de `<body>` vers `<head>`
- ✅ JSON-LD enrichi : ajout `url`, `image`, `author`

### page.tsx
- ✅ Suppression `"use client"` → page devient Server Component
- ✅ Suppression des états form (`email`, `name`, `status`, `handleSubmit`)
- ✅ Suppression des props form transmises à HeroSection et SubscribeSection
- ✅ Ajout `<main id="main-content">` (accessibilité + skip link)
- ✅ `<FooterSection>` sorti du `<main>` (sémantique correcte)

### HeroSection.tsx
- ✅ Interface simplifiée (plus de props form)
- ✅ Formulaire inline supprimé (était dupliqué avec SubscribeSection)
- ✅ H1 en français : "SCULPT YOUR" → "FORGE TON"
- ✅ Mots animés français : ["CORPS.", "PHYSIQUE.", "NIVEAU.", "POTENTIEL.", "MENTAL.", "OBJECTIF."]
- ✅ Subtitle : ajout keyword "musculation" + correction "séance après séance"
- ✅ Nav link : "Fonctionnalites" → "Fonctionnalités"
- ✅ Suppression `id="main-content"` du `<header>` (maintenant sur `<main>` dans page.tsx)

---

## Groupe B — Accents manquants

### features.ts
- ✅ "Cree" → "Crée", "seances" → "séances", "equipement" → "équipement"
- ✅ "detailles" → "détaillés", "instantanement" → "instantanément", "telephone" → "téléphone"
- ✅ "Pensee" → "Pensée", "passees" → "passées", "duree" → "durée", "Definis" → "Définis"
- ✅ "wifi a" → "wifi à", "s'adapte a" → "s'adapte à"

### pricing.ts
- ✅ "illimites" → "illimités", "avancees" → "avancées", "donnees" → "données"
- ✅ "Acces" → "Accès", "nouveautes" → "nouveautés", "avant-premiere" → "avant-première"
- ✅ "Economiser" → "Économiser"
- ✅ Prix Pro Annuel : ajout `badge: "soit 1,67€/mois"` pour clarté (économie visible)
- ✅ `badge?` ajouté à l'interface `PricingPlan`

### SubscribeSection.tsx
- ✅ Rendu autonome (useState + handleSubmit internes, plus de props)
- ✅ "Pret a progresser" → "Prêt à progresser"
- ✅ "etre informe" → "être informé", "acces anticipe" → "accès anticipé"
- ✅ "Prenom" → "Prénom" (label + placeholder)
- ✅ Messages d'état : "reussie" → "réussie", "Verifie" → "Vérifie", "Reessaie" → "Réessaie"
- ✅ "Desabonnement" → "Désabonnement", "confidentialite" → "confidentialité"

### FooterSection.tsx
- ✅ "Fonctionnalites" → "Fonctionnalités", "Confidentialite" → "Confidentialité"
- ✅ "reserves" → "réservés"

---

## Groupe C — UX Fixes

### PricingSection.tsx
- ✅ Boutons `<button>` sans onClick → `<a href="#download">` fonctionnels
- ✅ "pret" → "prêt"
- ✅ Affichage du badge `plan.badge` (ex: "soit 1,67€/mois") sous le prix Pro Annuel
- ✅ Reste Server Component (pas besoin de "use client")

### SocialProof.tsx
- ✅ Suppression du `FALLBACK = 342` trompeur
- ✅ En cas d'erreur API : message neutre "Rejoins les premiers inscrits" (pas de faux nombre)
- ✅ Ajout état `error` séparé de `loading`

### ThemeToggle.tsx
- ✅ Déjà conforme : `aria-label="Changer le theme clair/sombre"` présent

---

## Impact attendu
- **SEO** : page SSR (crawlable), H1 avec "musculation", descriptions avec accents corrects, JSON-LD enrichi dans `<head>`
- **Accessibilité** : `<main>` landmark, `aria-label` sur toggle, boutons pricing fonctionnels
- **UX** : un seul formulaire d'inscription (SubscribeSection), fallback SocialProof honnête, prix Pro Annuel clarifiés, liens sociaux dans footer
- **Qualité** : zéro erreur TypeScript
